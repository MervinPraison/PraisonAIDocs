---
title: "Deploy"
sidebarTitle: "Overview"
description: "Deploy PraisonAI agents as APIs"
icon: "server"
---

Deploy agents as HTTP APIs or MCP servers with one command.

## CLI

```bash
pip install praisonai
export OPENAI_API_KEY="your-key"

python -m praisonai --init "helpful assistant"
python -m praisonai --serve --port 8005
```

**Output:**
```
📄 Loading workflow from: agents.yaml
🚀 Starting PraisonAI API server...
   Host: 127.0.0.1
   Port: 8005
🚀 Multi-Agent HTTP API available at http://127.0.0.1:8005/agents
✅ FastAPI server started at http://127.0.0.1:8005
📚 API documentation available at http://127.0.0.1:8005/docs
```

## Python

```python
from praisonaiagents import Agent

agent = Agent(instructions="You are a helpful assistant.", llm="gpt-4o-mini")
agent.launch(path="/ask", port=8000)
```

**Output:**
```
🚀 Agent 'Agent' available at http://0.0.0.0:8000
✅ FastAPI server started at http://0.0.0.0:8000
📚 API documentation available at http://0.0.0.0:8000/docs
🔌 Available endpoints: /ask
```

## Test

```bash
curl -X POST http://localhost:8000/ask \
  -H "Content-Type: application/json" \
  -d '{"query": "Hello"}'
```

## Deploy Options

| Method | Use Case |
|--------|----------|
| [CLI Deploy](./deploy-cli) | Deploy from agents.yaml |
| [SDK Deploy](./deploy-sdk) | Programmatic deployment |
| [Multi-Agent](./multi-agents-deploy) | Multiple agents on one server |
| [MCP Server](./mcp-server-deploy) | Model Context Protocol |
| [Remote Agents](./remote-agents) | Network-accessible agents |
| [AWS](./aws) | Deploy to AWS |
| [Google Cloud](./googlecloud) | Deploy to GCP |
