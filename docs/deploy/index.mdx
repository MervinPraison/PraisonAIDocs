---
title: "Deploy"
sidebarTitle: "Overview"
description: "Deploy PraisonAI agents using praisonai deploy or as servers"
icon: "rocket"
---

Deploy agents to cloud providers, Docker, or run as local servers.

## Quick Start - praisonai deploy

```bash
pip install praisonai
export OPENAI_API_KEY="your-key"

# Initialize deploy config
praisonai deploy init --file agents.yaml --type api

# Deploy
praisonai deploy run --file agents.yaml
```

**Expected Output:**
```
🚀 Starting deployment...

✅ Deployment successful!
🔗 URL: http://127.0.0.1:8005
```

## Deploy Options

<CardGroup cols={2}>
  <Card title="Deploy CLI (praisonai deploy)" icon="terminal" href="/docs/deploy/cli/index">
    Deploy to API, Docker, AWS, Azure, or GCP
  </Card>
  <Card title="Servers (Deploy as a Server)" icon="server" href="/docs/deploy/servers/agents">
    Run agents as HTTP, MCP, A2A, or AGUI servers
  </Card>
  <Card title="API Endpoints" icon="plug" href="/docs/deploy/api/agents-api">
    HTTP API reference for all server types
  </Card>
</CardGroup>

## praisonai deploy Commands

| Command | Description |
|---------|-------------|
| `praisonai deploy run` | Execute deployment |
| `praisonai deploy init` | Generate sample agents.yaml |
| `praisonai deploy validate` | Validate configuration |
| `praisonai deploy plan` | Show deployment plan |
| `praisonai deploy doctor` | Check readiness |
| `praisonai deploy status` | Show deployment status |
| `praisonai deploy destroy` | Destroy deployment |

## Deploy Targets (praisonai deploy)

| Target | Command | Page |
|--------|---------|------|
| API Server | `praisonai deploy run --type api` | [API](./cli/api) |
| Docker | `praisonai deploy run --type docker` | [Docker](./cli/docker) |
| AWS | `praisonai deploy run --type cloud --provider aws` | [AWS](./cli/aws) |
| Azure | `praisonai deploy run --type cloud --provider azure` | [Azure](./cli/azure) |
| GCP | `praisonai deploy run --type cloud --provider gcp` | [GCP](./cli/gcp) |

## Servers (Deploy as a Server)

| Server Type | Use Case | Page |
|-------------|----------|------|
| Agents Server | HTTP API via `praisonai --serve` or `Agent.launch()` | [Agents](./servers/agents) |
| Tools MCP | Expose tools as MCP server | [Tools MCP](./servers/tools-mcp) |
| A2A Server | Agent-to-Agent communication | [A2A](./servers/a2a) |
| AGUI Server | CopilotKit frontend integration | [AGUI](./servers/agui) |

## Quick Examples

<Tabs>
  <Tab title="praisonai deploy">
```bash
# Deploy to local API server
praisonai deploy run --type api

# Deploy to Docker
praisonai deploy run --type docker

# Deploy to AWS
praisonai deploy run --type cloud --provider aws
```
  </Tab>
  <Tab title="praisonai --serve">
```bash
praisonai --init "helpful assistant"
praisonai --serve --port 8000
```
  </Tab>
  <Tab title="Python SDK">
```python
from praisonaiagents import Agent

agent = Agent(instructions="You are helpful")
agent.launch(port=8000)
```
  </Tab>
</Tabs>
