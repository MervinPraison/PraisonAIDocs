---
title: "Markdown Agent"
sidebarTitle: "Markdown"
description: "Learn how to create AI agents for generating and formatting content in Markdown."
icon: "markdown"
---

```mermaid
flowchart LR
    In[Request] --> Agent[Markdown Agent]
    Agent --> Out[Markdown Output]
    
    style In fill:#8B0000,color:#fff
    style Agent fill:#2E8B57,color:#fff
    style Out fill:#8B0000,color:#fff
```

Content generation agent that outputs properly formatted Markdown.

**Agents: 1** — Single agent for content generation with Markdown formatting.

## Workflow

1. Receive content request
2. Generate content with LLM
3. Format output as Markdown
4. Return structured document

## Setup

```bash
pip install praisonaiagents praisonai
export OPENAI_API_KEY="your-key"
```

## Run — Python

```python
from praisonaiagents import Agent

agent = Agent(
    name="MarkdownWriter",
    instructions="You are a Markdown agent. Always output in proper Markdown format with headers, lists, and code blocks."
)

result = agent.start("Write a README for a Python web scraping project")
print(result)
```

## Run — CLI

```bash
# Generate markdown content
praisonai "Write a README for a Python project"

# Save to file
praisonai "Write API documentation" > api_docs.md
```

## Run — agents.yaml

```yaml
framework: praisonai
topic: Documentation Generation
roles:
  markdown_writer:
    role: Markdown Content Specialist
    goal: Generate well-formatted Markdown content
    backstory: You are an expert technical writer
    tasks:
      write_docs:
        description: Write a README for a Python web scraping project
        expected_output: A complete README.md with installation, usage, and examples
```

```bash
praisonai agents.yaml
```

## Serve API

```python
from praisonaiagents import Agent

agent = Agent(
    name="MarkdownWriter",
    instructions="You are a Markdown agent. Always output in proper Markdown format."
)

agent.launch(port=8080)
```

```bash
# Test the API
curl -X POST http://localhost:8080/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Write a changelog for version 2.0"}'
```

## Features

| Feature | Supported |
|---------|-----------|
| Workflow | ✅ Single step |
| DB Persistence | ❌ |
| Vector Retrieval | ❌ |
| Observability | ✅ `--verbose` |
| Tools | ❌ |
| Resumability | ❌ |
| Structured Output | ✅ Markdown format |

## Next Steps

- [Single Agent](/agents/single) for basic content generation
- [Prompt Chaining](/features/promptchaining) for multi-step documents
- [Structured Output](/features/structured) for JSON responses
