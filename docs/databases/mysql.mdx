---
title: "MySQL"
description: "MySQL conversation store for PraisonAI"
icon: "database"
---

# MySQL

Store conversation history in MySQL.

## Setup

```bash
# Docker
docker run -d -p 3306:3306 -e MYSQL_ROOT_PASSWORD=password -e MYSQL_DATABASE=praisonai mysql:8

# Install driver
pip install pymysql
```

## Quick Start (Agent)

Use MySQL for conversation persistence with an agent:

```python
from praisonaiagents import Agent, db

# Create agent with MySQL conversation store
agent = Agent(
    name="Assistant",
    instructions="You are a helpful assistant.",
    db=db(database_url="mysql+pymysql://root:password@localhost:3306/praisonai"),
    session_id="my-session"
)

agent.chat("Hello!")
```

## Advanced Usage (Direct Store)

```python
from praisonai.persistence.factory import create_conversation_store

store = create_conversation_store(
    "mysql",
    url="mysql+pymysql://root:password@localhost:3306/praisonai"
)
```

## CLI

```bash
praisonai persistence doctor --conversation-url "mysql+pymysql://root:password@localhost:3306/praisonai"
```

## Configuration

| Option | Description |
|--------|-------------|
| `url` | MySQL connection URL |
| `pool_size` | Connection pool size (default: 5) |
| `table_prefix` | Table name prefix (default: `praison_`) |
