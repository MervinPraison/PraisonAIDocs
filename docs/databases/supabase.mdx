---
title: "Supabase"
description: "Supabase conversation store for PraisonAI"
icon: "bolt"
---

# Supabase

Open-source Firebase alternative with PostgreSQL.

## Setup

```bash
pip install supabase
export SUPABASE_URL=your_url
export SUPABASE_KEY=your_key
```

## Quick Start

Use Supabase for conversation persistence with an agent:

```python
from praisonaiagents import Agent
import os

agent = Agent(
    name="Assistant",
    instructions="You are a helpful assistant.",
    memory={
        "db": f"supabase://{os.getenv('SUPABASE_URL')}",
        "session_id": "my-session"
    }
)

response = agent.start("Hello!")
print(response)
```

<Tip>
`praisonai persistence doctor` auto-detects `*.supabase.co` / `*.supabase.com` REST hosts as the `supabase` backend from the URL — no `--*-backend` flag needed. See [Backend Auto-Detection](/cli/persistence#backend-auto-detection).
</Tip>
