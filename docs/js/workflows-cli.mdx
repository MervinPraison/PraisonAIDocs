---
title: "Workflows CLI"
sidebarTitle: "Workflows CLI"
description: "CLI commands for workflows"
icon: "terminal"
---

```bash
# Run basic workflow example
npx ts-node examples/workflows/basic-workflow.ts
```

```bash
# Run parallel execution example
npx ts-node examples/workflows/parallel.ts
```

```bash
# Run conditional routing example
npx ts-node examples/workflows/routing.ts
```

```bash
# Run loop example
npx ts-node examples/workflows/loop.ts
```

```bash
# Run error handling example
npx ts-node examples/workflows/error-handling.ts
```
