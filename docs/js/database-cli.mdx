---
title: "Database CLI"
sidebarTitle: "Database CLI"
description: "CLI commands for database operations in PraisonAI TypeScript"
icon: "terminal"
---

# Database CLI Commands

The `praisonai-ts` CLI provides the `db` command for database adapter management.

## View Database Info

```bash
# Show database adapter information
praisonai-ts db info

# List available adapters
praisonai-ts db adapters --json
```

**Example Output:**
```json
{
  "success": true,
  "data": {
    "adapters": [
      { "name": "sqlite", "description": "SQLite database", "available": true },
      { "name": "redis", "description": "Redis (Upstash)", "available": true },
      { "name": "postgres", "description": "PostgreSQL (Neon)", "available": true },
      { "name": "memory", "description": "In-memory adapters", "available": true }
    ]
  }
}
```

## Available Adapters

| Adapter | Description |
|---------|-------------|
| `SQLiteAdapter` | SQLite database |
| `UpstashRedisAdapter` | Upstash Redis |
| `NeonPostgresAdapter` | Neon PostgreSQL |
| `MemoryAdapter` | In-memory storage |

## SDK Usage

For programmatic database usage:

```typescript
import { createSQLiteAdapter, createUpstashRedis, createNeonPostgres } from 'praisonai';

// SQLite
const sqlite = createSQLiteAdapter({ filename: './data.db' });

// Redis
const redis = createUpstashRedis({
  url: process.env.UPSTASH_REDIS_URL,
  token: process.env.UPSTASH_REDIS_TOKEN
});

// PostgreSQL
const pg = createNeonPostgres({
  connectionString: process.env.DATABASE_URL
});
```

For more details, see the [Database SDK documentation](/docs/js/database).
