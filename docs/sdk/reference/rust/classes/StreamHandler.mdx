---
title: "Stream Handler • Rust AI Agent SDK"
sidebarTitle: "Stream Handler"
description: "StreamHandler: Handler for managing stream callbacks."
icon: "brackets-curly"
---

# StreamHandler

> Defined in the [**streaming**](../modules/streaming) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Handler for managing stream callbacks.

## Fields

| Name | Type | Description |
|------|------|-------------|
| `callbacks` | `Vec&lt;Box&lt;dyn StreamCallback&gt;&gt;` | - |

## Methods

### `new`

```rust
fn new() -> Self
```

Create a new stream handler

### `add_callback`

```rust
fn add_callback(&mut self, callback: impl StreamCallback + 'static) -> ()
```

Add a callback

**Parameters:**

| Name | Type |
|------|------|
| `callback` | `impl StreamCallback + 'static` |

### `emit`

```rust
fn emit(&self, event: &StreamEvent) -> ()
```

Emit an event to all callbacks

**Parameters:**

| Name | Type |
|------|------|
| `event` | `&StreamEvent` |

### `callback_count`

```rust
fn callback_count(&self) -> usize
```

Get callback count

