---
title: "Knowledge Store Protocol • Rust AI Agent SDK"
sidebarTitle: "Knowledge Store Protocol"
description: "KnowledgeStoreProtocol: Protocol for knowledge store backends."
icon: "brackets-curly"
---

# KnowledgeStoreProtocol

> Defined in the [**knowledge**](../modules/knowledge) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Protocol for knowledge store backends.

## Methods

### `search`

```rust
async fn search(
        &self,
        query: &str,
        user_id: Option<&str>,
        agent_id: Option<&str>,
        limit: usize,
    ) -> Result<SearchResult>
```

Search for relevant content

**Parameters:**

| Name | Type |
|------|------|
| `query` | `&str` |
| `user_id` | `Option&lt;&str&gt;` |
| `agent_id` | `Option&lt;&str&gt;` |
| `limit` | `usize` |

### `add`

```rust
async fn add(
        &mut self,
        content: &str,
        user_id: Option<&str>,
        agent_id: Option<&str>,
        metadata: Option<HashMap<String, String>>,
    ) -> Result<AddResult>
```

Add content to the store

**Parameters:**

| Name | Type |
|------|------|
| `content` | `&str` |
| `user_id` | `Option&lt;&str&gt;` |
| `agent_id` | `Option&lt;&str&gt;` |
| `metadata` | `Option&lt;HashMap&lt;String` |

### `get`

```rust
async fn get(&self, item_id: &str) -> Result<Option<SearchResultItem>>
```

Get item by ID

**Parameters:**

| Name | Type |
|------|------|
| `item_id` | `&str` |

### `get_all`

```rust
async fn get_all(
        &self,
        user_id: Option<&str>,
        agent_id: Option<&str>,
        limit: usize,
    ) -> Result<SearchResult>
```

Get all items

**Parameters:**

| Name | Type |
|------|------|
| `user_id` | `Option&lt;&str&gt;` |
| `agent_id` | `Option&lt;&str&gt;` |
| `limit` | `usize` |

### `update`

```rust
async fn update(&mut self, item_id: &str, content: &str) -> Result<AddResult>
```

Update an item

**Parameters:**

| Name | Type |
|------|------|
| `item_id` | `&str` |
| `content` | `&str` |

### `delete`

```rust
async fn delete(&mut self, item_id: &str) -> Result<bool>
```

Delete an item

**Parameters:**

| Name | Type |
|------|------|
| `item_id` | `&str` |

### `delete_all`

```rust
async fn delete_all(&mut self, user_id: Option<&str>, agent_id: Option<&str>) -> Result<bool>
```

Delete all items

**Parameters:**

| Name | Type |
|------|------|
| `user_id` | `Option&lt;&str&gt;` |
| `agent_id` | `Option&lt;&str&gt;` |

