---
title: "Caching Config • Rust AI Agent SDK"
sidebarTitle: "Caching Config"
description: "CachingConfig: Configuration for caching behavior"
icon: "brackets-curly"
---

# CachingConfig

> Defined in the [**Configuration**](../modules/config) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Configuration for caching behavior

## Fields

| Name | Type | Description |
|------|------|-------------|
| `enabled` | `bool` | Enable response caching |
| `prompt_caching` | `bool` | Enable prompt caching (provider-specific) |
| `ttl_secs` | `Option&lt;u64&gt;` | Cache TTL in seconds |

## Methods

### `new`

```rust
fn new() -> Self
```

Create a new caching config

### `disabled`

```rust
fn disabled(mut self) -> Self
```

Disable caching

### `with_prompt_caching`

```rust
fn with_prompt_caching(mut self) -> Self
```

Enable prompt caching

### `ttl`

```rust
fn ttl(mut self, secs: u64) -> Self
```

Set TTL

**Parameters:**

| Name | Type |
|------|------|
| `secs` | `u64` |



---

## Related Documentation

<CardGroup cols={2}>
  <Card title="Rust Installation" icon="gear" href="/docs/rust/installation" />
  <Card title="Rust Overview" icon="book-open" href="/docs/rust/overview" />
</CardGroup>
