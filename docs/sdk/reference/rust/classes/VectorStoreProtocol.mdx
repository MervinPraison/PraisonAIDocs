---
title: "Vector Store Protocol • Rust AI Agent SDK"
sidebarTitle: "Vector Store Protocol"
description: "VectorStoreProtocol: Protocol for vector store implementations."
icon: "brackets-curly"
---

# VectorStoreProtocol

> Defined in the [**knowledge**](../modules/knowledge) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Protocol for vector store implementations.

## Methods

### `add`

```rust
async fn add(&mut self, record: VectorRecord) -> Result<String>
```

Add a record to the store

**Parameters:**

| Name | Type |
|------|------|
| `record` | `VectorRecord` |

### `search`

```rust
async fn search(&self, query_embedding: &[f32], limit: usize) -> Result<Vec<SearchResultItem>>
```

Search for similar records

**Parameters:**

| Name | Type |
|------|------|
| `query_embedding` | `&[f32]` |
| `limit` | `usize` |

### `get`

```rust
async fn get(&self, id: &str) -> Result<Option<VectorRecord>>
```

Get a record by ID

**Parameters:**

| Name | Type |
|------|------|
| `id` | `&str` |

### `delete`

```rust
async fn delete(&mut self, id: &str) -> Result<bool>
```

Delete a record by ID

**Parameters:**

| Name | Type |
|------|------|
| `id` | `&str` |

### `get_all`

```rust
async fn get_all(&self, limit: usize) -> Result<Vec<VectorRecord>>
```

Get all records

**Parameters:**

| Name | Type |
|------|------|
| `limit` | `usize` |

### `clear`

```rust
async fn clear(&mut self) -> Result<()>
```

Clear all records

### `len`

```rust
fn len(&self) -> usize
```

Get record count

### `is_empty`

```rust
fn is_empty(&self) -> bool
```

Check if empty

