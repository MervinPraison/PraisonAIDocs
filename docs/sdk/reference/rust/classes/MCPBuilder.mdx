---
title: "M C P Builder • Rust AI Agent SDK"
sidebarTitle: "M C P Builder"
description: "MCPBuilder: Builder for MCP"
icon: "brackets-curly"
---

# MCPBuilder

> Defined in the [**mcp**](../modules/mcp) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Builder for MCP

## Fields

| Name | Type | Description |
|------|------|-------------|
| `config` | `MCPConfig` | - |
| `tools` | `Vec&lt;MCPTool&gt;` | - |

## Methods

### `name`

```rust
fn name(mut self, name: impl Into<String>) -> Self
```

Set server name

**Parameters:**

| Name | Type |
|------|------|
| `name` | `impl Into&lt;String&gt;` |

### `server`

```rust
fn server(mut self, command: impl Into<String>, args: &[&str]) -> Self
```

Set stdio server

**Parameters:**

| Name | Type |
|------|------|
| `command` | `impl Into&lt;String&gt;` |
| `args` | `&[&str]` |

### `http`

```rust
fn http(mut self, url: impl Into<String>) -> Self
```

Set HTTP server

**Parameters:**

| Name | Type |
|------|------|
| `url` | `impl Into&lt;String&gt;` |

### `websocket`

```rust
fn websocket(mut self, url: impl Into<String>) -> Self
```

Set WebSocket server

**Parameters:**

| Name | Type |
|------|------|
| `url` | `impl Into&lt;String&gt;` |

### `config`

```rust
fn config(mut self, config: MCPConfig) -> Self
```

Set config

**Parameters:**

| Name | Type |
|------|------|
| `config` | `MCPConfig` |

### `security`

```rust
fn security(mut self, security: SecurityConfig) -> Self
```

Set security

**Parameters:**

| Name | Type |
|------|------|
| `security` | `SecurityConfig` |

### `tool`

```rust
fn tool(mut self, tool: MCPTool) -> Self
```

Add a tool (for testing)

**Parameters:**

| Name | Type |
|------|------|
| `tool` | `MCPTool` |

### `build`

```rust
fn build(self) -> Result<MCP>
```

Build the MCP client



---

## Related Documentation

<CardGroup cols={2}>
  <Card title="Rust Agent Guide" icon="robot" href="/docs/rust/agent" />
  <Card title="Rust Quickstart" icon="rocket" href="/docs/rust/quickstart" />
</CardGroup>
