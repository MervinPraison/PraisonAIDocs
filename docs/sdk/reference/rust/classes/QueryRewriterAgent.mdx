---
title: "Query Rewriter Agent • Rust AI Agent SDK"
sidebarTitle: "Query Rewriter Agent"
description: "QueryRewriterAgent: Agent for rewriting queries"
icon: "brackets-curly"
---

# QueryRewriterAgent

> Defined in the [**Specialized Agents**](../modules/specialized_agents) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Agent for rewriting queries

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#8B0000', 'primaryTextColor': '#fff', 'primaryBorderColor': '#710101', 'lineColor': '#189AB4', 'secondaryColor': '#189AB4', 'tertiaryColor': '#fff' }}}%%

graph LR
    input["Input Data"] --> agent["Agent: QueryRewriterAgent"]
    agent --> output["Output Result"]
    style agent fill:#8B0000,color:#fff
    style input fill:#8B0000,color:#fff
    style output fill:#8B0000,color:#fff
```

## Fields

| Name | Type | Description |
|------|------|-------------|
| `config` | `QueryRewriterConfig` | - |

## Methods

### `new`

```rust
fn new() -> QueryRewriterAgentBuilder
```

Create a new builder

### `name`

```rust
fn name(&self) -> &str
```

Get agent name

### `model`

```rust
fn model(&self) -> &str
```

Get model

### `expand_abbreviations`

```rust
fn expand_abbreviations(&self, query: &str) -> String
```

Expand abbreviations in query

**Parameters:**

| Name | Type |
|------|------|
| `query` | `&str` |

### `detect_strategy`

```rust
fn detect_strategy(&self, query: &str, has_chat_history: bool) -> RewriteStrategy
```

Detect the best rewriting strategy for a query

**Parameters:**

| Name | Type |
|------|------|
| `query` | `&str` |
| `has_chat_history` | `bool` |

### `rewrite_sync`

```rust
fn rewrite_sync(
        &self,
        query: &str,
        strategy: RewriteStrategy,
        chat_history: Option<&[HashMap<String, String>]>,
        context: Option<&str>,
        num_queries: Option<usize>,
    ) -> RewriteResult
```

Rewrite a query (sync placeholder - actual implementation would use LLM)

**Parameters:**

| Name | Type |
|------|------|
| `query` | `&str` |
| `strategy` | `RewriteStrategy` |
| `chat_history` | `Option&lt;&[HashMap&lt;String` |
| `context` | `Option&lt;&str&gt;` |
| `num_queries` | `Option&lt;usize&gt;` |


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/ARC-Solutions/praisonai-rust/blob/main/praisonai/src/specialized_agents.rs#L722">
  `praisonai/src/specialized_agents.rs` at line 722
</Card>



---

## Related Documentation

<CardGroup cols={2}>
  <Card title="Agents Concept" icon="robot" href="/docs/concepts/agents" />
  <Card title="Single Agent Guide" icon="book-open" href="/docs/guides/single-agent" />
  <Card title="Multi-Agent Guide" icon="users" href="/docs/guides/multi-agent" />
  <Card title="Agent Configuration" icon="gear" href="/docs/configuration/agent-config" />
  <Card title="Auto Agents" icon="wand-magic-sparkles" href="/docs/features/autoagents" />
</CardGroup>
