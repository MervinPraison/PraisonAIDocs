---
title: "Stream Collector • Rust AI Agent SDK"
sidebarTitle: "Stream Collector"
description: "StreamCollector: Collects stream events and accumulated content."
icon: "brackets-curly"
---

# StreamCollector

> Defined in the [**streaming**](../modules/streaming) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Collects stream events and accumulated content.

## Fields

| Name | Type | Description |
|------|------|-------------|
| `events` | `Vec&lt;StreamEvent&gt;` | Collected events |
| `content` | `String` | Accumulated content |
| `metrics` | `StreamMetrics` | Metrics |

## Methods

### `new`

```rust
fn new() -> Self
```

Create a new collector

### `process`

```rust
fn process(&mut self, event: StreamEvent) -> ()
```

Process an event

**Parameters:**

| Name | Type |
|------|------|
| `event` | `StreamEvent` |

### `get_content`

```rust
fn get_content(&self) -> &str
```

Get final content

### `event_count`

```rust
fn event_count(&self) -> usize
```

Get event count

### `is_complete`

```rust
fn is_complete(&self) -> bool
```

Check if streaming completed successfully

### `has_error`

```rust
fn has_error(&self) -> bool
```

Check if there was an error

### `get_error`

```rust
fn get_error(&self) -> Option<&str>
```

Get error message if any


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/ARC-Solutions/praisonai-rust/blob/main/praisonai/src/streaming/mod.rs#L424">
  `praisonai/src/streaming/mod.rs` at line 424
</Card>
