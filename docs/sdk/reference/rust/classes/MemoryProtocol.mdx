---
title: "Memory Protocol • Rust AI Agent SDK"
sidebarTitle: "Memory Protocol"
description: "MemoryProtocol: Protocol for memory implementations."
icon: "brackets-curly"
---

# MemoryProtocol

> Defined in the [**protocols**](../modules/protocols) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Protocol for memory implementations.

## Methods

### `store`

```rust
async fn store(&mut self, role: &str, content: &str) -> Result<()>
```

Store a message in memory

**Parameters:**

| Name | Type |
|------|------|
| `role` | `&str` |
| `content` | `&str` |

### `history`

```rust
async fn history(&self) -> Result<Vec<MemoryMessage>>
```

Get conversation history

### `clear`

```rust
async fn clear(&mut self) -> Result<()>
```

Clear memory

### `search`

```rust
async fn search(&self, query: &str, limit: usize) -> Result<Vec<MemoryMessage>>
```

Search memory

**Parameters:**

| Name | Type |
|------|------|
| `query` | `&str` |
| `limit` | `usize` |



---

## Related Documentation

<CardGroup cols={2}>
  <Card title="Rust Memory Guide" icon="brain" href="/docs/rust/memory" />
  <Card title="Rust Agent Guide" icon="robot" href="/docs/rust/agent" />
</CardGroup>
