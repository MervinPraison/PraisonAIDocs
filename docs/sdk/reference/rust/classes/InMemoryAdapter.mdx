---
title: "InMemoryAdapter"
description: "In-memory adapter (default)"
icon: "brackets-curly"
---

# InMemoryAdapter

> Defined in the [**memory**](../modules/memory) module.

<Badge color="orange">Rust SDK</Badge>

In-memory adapter (default)

## Fields

| Name | Type | Description |
|------|------|-------------|
| `history` | `ConversationHistory` | - |

## Methods

### `new`

```rust
fn new(max_messages: usize) -> Self
```

Create a new in-memory adapter

**Parameters:**

| Name | Type |
|------|------|
| `max_messages` | `usize` |

