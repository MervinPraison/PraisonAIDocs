---
title: "Session Store • Rust AI Agent SDK"
sidebarTitle: "Session Store"
description: "SessionStore: Session store trait for different storage backends"
icon: "brackets-curly"
---

# SessionStore

> Defined in the [**session**](../modules/session) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Session store trait for different storage backends

## Methods

### `load`

```rust
fn load(&self, session_id: &str) -> Result<SessionData>
```

Load session data

**Parameters:**

| Name | Type |
|------|------|
| `session_id` | `&str` |

### `save`

```rust
fn save(&self, session: &SessionData) -> Result<()>
```

Save session data

**Parameters:**

| Name | Type |
|------|------|
| `session` | `&SessionData` |

### `exists`

```rust
fn exists(&self, session_id: &str) -> bool
```

Check if session exists

**Parameters:**

| Name | Type |
|------|------|
| `session_id` | `&str` |

### `delete`

```rust
fn delete(&self, session_id: &str) -> Result<()>
```

Delete a session

**Parameters:**

| Name | Type |
|------|------|
| `session_id` | `&str` |

### `list`

```rust
fn list(&self, limit: usize) -> Result<Vec<SessionInfo>>
```

List all sessions

**Parameters:**

| Name | Type |
|------|------|
| `limit` | `usize` |

