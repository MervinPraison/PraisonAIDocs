---
title: "Plan Storage • Rust AI Agent SDK"
sidebarTitle: "Plan Storage"
description: "PlanStorage: Storage for plans."
icon: "brackets-curly"
---

# PlanStorage

> Defined in the [**planning**](../modules/planning) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Storage for plans.

## Fields

| Name | Type | Description |
|------|------|-------------|
| `plans` | `HashMap&lt;String` | Plans by ID |
| `path` | `Option&lt;PathBuf&gt;` | Storage path |

## Methods

### `new`

```rust
fn new() -> Self
```

Create a new storage.

### `with_path`

```rust
fn with_path(path: impl Into<PathBuf>) -> Self
```

Create with file path.

**Parameters:**

| Name | Type |
|------|------|
| `path` | `impl Into&lt;PathBuf&gt;` |

### `save`

```rust
fn save(&mut self, plan: Plan) -> ()
```

Save a plan.

**Parameters:**

| Name | Type |
|------|------|
| `plan` | `Plan` |

### `load`

```rust
fn load(&self, id: &str) -> Option<&Plan>
```

Load a plan by ID.

**Parameters:**

| Name | Type |
|------|------|
| `id` | `&str` |

### `load_mut`

```rust
fn load_mut(&mut self, id: &str) -> Option<&mut Plan>
```

Load mutable plan by ID.

**Parameters:**

| Name | Type |
|------|------|
| `id` | `&str` |

### `delete`

```rust
fn delete(&mut self, id: &str) -> Option<Plan>
```

Delete a plan.

**Parameters:**

| Name | Type |
|------|------|
| `id` | `&str` |

### `list`

```rust
fn list(&self) -> Vec<&Plan>
```

List all plans.

### `count`

```rust
fn count(&self) -> usize
```

Get plan count.

### `persist`

```rust
fn persist(&self) -> std::io::Result<()>
```

Save to file.

### `restore`

```rust
fn restore(&mut self) -> std::io::Result<()>
```

Load from file.


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/ARC-Solutions/praisonai-rust/blob/main/praisonai/src/planning/mod.rs#L402">
  `praisonai/src/planning/mod.rs` at line 402
</Card>



---

## Related Documentation

<CardGroup cols={2}>
  <Card title="RAG Concept" icon="magnifying-glass" href="/docs/concepts/rag" />
  <Card title="RAG Overview" icon="database" href="/docs/rag/overview" />
  <Card title="Chunking Strategies" icon="scissors" href="/docs/rag/strategies/overview" />
</CardGroup>
