---
title: "Budget Allocation • Rust AI Agent SDK"
sidebarTitle: "Budget Allocation"
description: "BudgetAllocation: Budget allocation for different context components."
icon: "brackets-curly"
---

# BudgetAllocation

> Defined in the [**context**](../modules/context) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

Budget allocation for different context components.

## Fields

| Name | Type | Description |
|------|------|-------------|
| `total` | `usize` | Total budget in tokens |
| `system` | `usize` | Budget for system prompt |
| `history` | `usize` | Budget for conversation history |
| `tools` | `usize` | Budget for tools |
| `output_reserve` | `usize` | Reserved for output |

## Methods

### `new`

```rust
fn new(total: usize) -> Self
```

Create a new budget allocation

**Parameters:**

| Name | Type |
|------|------|
| `total` | `usize` |

### `with_ratios`

```rust
fn with_ratios(total: usize, system_pct: f64, history_pct: f64, tools_pct: f64) -> Self
```

Create with custom ratios

**Parameters:**

| Name | Type |
|------|------|
| `total` | `usize` |
| `system_pct` | `f64` |
| `history_pct` | `f64` |
| `tools_pct` | `f64` |


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/ARC-Solutions/praisonai-rust/blob/main/praisonai/src/context/mod.rs#L146">
  `praisonai/src/context/mod.rs` at line 146
</Card>
