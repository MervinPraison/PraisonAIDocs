---
title: "handoff() • Rust AI Agent SDK"
sidebarTitle: "handoff()"
description: "Create a handoff to another agent # Arguments * `target` - Name of the target agent * `message` - Optional handoff message"
icon: "function"
---

# handoff

> Defined in the [**Workflow Aliases**](../modules/workflow_aliases) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

```rust
fn handoff(target: impl Into<String>, message: Option<&str>) -> HandoffConfig
```

Create a handoff to another agent # Arguments * `target` - Name of the target agent * `message` - Optional handoff message

## Parameters

| Name | Type | Description |
|------|------|-------------|
| `target` | `impl Into&lt;String&gt;` | - |
| `message` | `Option&lt;&str&gt;` | - |

