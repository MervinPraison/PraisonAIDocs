---
title: "subscribe() • Rust AI Agent SDK"
sidebarTitle: "subscribe()"
description: "subscribe: Subscribe to events on the global bus"
icon: "function"
---

# subscribe

> Defined in the [**bus**](../modules/bus) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

```rust
fn subscribe(event_type: EventType, handler: F) -> usize
where
    F: Fn(&Event) + Send + Sync + 'static,
```

Subscribe to events on the global bus

## Parameters

| Name | Type | Description |
|------|------|-------------|
| `event_type` | `EventType` | - |
| `handler` | `F` | - |

