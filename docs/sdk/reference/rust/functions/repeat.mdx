---
title: "repeat() • Rust AI Agent SDK"
sidebarTitle: "repeat()"
description: "Create a repeat workflow step Executes the agent a fixed number of times. # Arguments * `agent` - The agent to execute * `times` - Number of times to..."
icon: "function"
---

# repeat

> Defined in the [**Workflow Aliases**](../modules/workflow_aliases) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

```rust
fn repeat(agent: Arc<crate::agent::Agent>, times: usize) -> crate::workflows::FlowStep
```

Create a repeat workflow step Executes the agent a fixed number of times. # Arguments * `agent` - The agent to execute * `times` - Number of times to repeat

## Parameters

| Name | Type | Description |
|------|------|-------------|
| `agent` | `Arc&lt;crate::agent::Agent&gt;` | - |
| `times` | `usize` | - |

