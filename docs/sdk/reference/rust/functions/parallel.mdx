---
title: "parallel() • Rust AI Agent SDK"
sidebarTitle: "parallel()"
description: "Create a parallel workflow step Executes multiple agents concurrently. # Arguments * `agents` - List of agents to execute in parallel"
icon: "function"
---

# parallel

> Defined in the [**Workflow Aliases**](../modules/workflow_aliases) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

```rust
fn parallel(agents: Vec<Arc<crate::agent::Agent>>) -> crate::workflows::FlowStep
```

Create a parallel workflow step Executes multiple agents concurrently. # Arguments * `agents` - List of agents to execute in parallel

## Parameters

| Name | Type | Description |
|------|------|-------------|
| `agents` | `Vec&lt;Arc&lt;crate::agent::Agent&gt;&gt;` | - |

