---
title: "subscribe_all() • Rust AI Agent SDK"
sidebarTitle: "subscribe_all()"
description: "subscribe_all: Subscribe to all events on the global bus"
icon: "function"
---

# subscribe_all

> Defined in the [**bus**](../modules/bus) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

```rust
fn subscribe_all(handler: F) -> usize
where
    F: Fn(&Event) + Send + Sync + 'static,
```

Subscribe to all events on the global bus

## Parameters

| Name | Type | Description |
|------|------|-------------|
| `handler` | `F` | - |

