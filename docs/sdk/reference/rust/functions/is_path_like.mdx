---
title: "is_path_like() • Rust AI Agent SDK"
sidebarTitle: "is_path_like()"
description: "is_path_like: Check if a string looks like a file path. O(1) operation."
icon: "function"
---

# is_path_like

> Defined in the [**Parse Utils**](../modules/parse_utils) module.

<Badge color="orange">Rust AI Agent SDK</Badge>

```rust
fn is_path_like(value: &str) -> bool
```

Check if a string looks like a file path. O(1) operation.

## Parameters

| Name | Type | Description |
|------|------|-------------|
| `value` | `&str` | - |

