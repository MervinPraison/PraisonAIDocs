---
title: "Is Path Like • Rust AI Agent SDK"
sidebarTitle: "Is Path Like"
description: "is_path_like: Check if a string looks like a file path. O(1) operation."
icon: "function"
---

# is_path_like

<div className="flex items-center gap-2">
  <Badge color="teal">Function</Badge>
</div>

> This function is defined in the [**parse_utils**](../modules/parse_utils) module.

Check if a string looks like a file path. O(1) operation.

## Signature

```python
def is_path_like(value: &str) -> bool
```

## Parameters

<ParamField query="value" type="&str" required={true}>
  No description available.
</ParamField>

### Returns

<ResponseField name="Returns" type="bool">
  The result of the operation.
</ResponseField>

## Usage

```python
use praisonai::parity::parse_utils::is_path_like;

assert!(is_path_like("docs/"));
assert!(is_path_like("./data.pdf"));
assert!(!is_path_like("verbose"));
```


## Uses

- `starts_with`
- `ends_with`
- `extension`
- `contains`
- `rsplit`
- `next`
- `unwrap_or`
- `to_lowercase`


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai-rust/praisonai/src/parity/parse_utils.rs#L60">
  `praisonai/src/parity/parse_utils.rs` at line 60
</Card>
