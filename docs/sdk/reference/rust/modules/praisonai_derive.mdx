---
title: "Derive Macros | Rust AI Agent"
description: "Procedural macros for PraisonAI  This crate provides the `#[tool]` attribute macro for defining tools.  # Example  ```rust,ignore use..."
icon: "file-code"
---


<Badge color="orange">Rust AI Agent SDK</Badge>

Procedural macros for PraisonAI

This crate provides the `#[tool]` attribute macro for defining tools.


```rust,ignore
use praisonai::tool;

#[tool(description = "Search the web for information")]
async fn search_web(query: String) -&gt; String &#123;
format!("Results for: &#123;&#125;", query)
&#125;
```

## Import

```rust
use praisonai_derive::*;
```

## Functions

<CardGroup cols={2}>
  <Card title="#[tool] Macro" icon="function" href="../functions/tool">
    The `#[tool]` attribute macro for defining tools. This macro transforms a function into a tool that can be used by agents. # Attributes -...
  </Card>
</CardGroup>

