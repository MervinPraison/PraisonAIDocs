---
title: "recommend_pattern_llm"
description: "API documentation for WorkflowAutoGenerator.recommend_pattern_llm"
icon: "function"
---

# WorkflowAutoGenerator.recommend_pattern_llm

*Class: [WorkflowAutoGenerator](../classes/WorkflowAutoGenerator)*

Use LLM to recommend the best workflow pattern with reasoning.

Args:
    topic: The task description (uses self.topic if not provided)
    
Returns:
    PatternRecommendation: Pattern with reasoning and confidence score

## Signature

```python
def recommend_pattern_llm(topic: str) -> PatternRecommendation
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `topic` | `str` | - |

## Returns

Type: `PatternRecommendation`
