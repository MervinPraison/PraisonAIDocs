---
title: "DeploymentScheduler.start"
description: "API documentation for DeploymentScheduler.start"
---

# DeploymentScheduler.start

*Class: [DeploymentScheduler](../classes/DeploymentScheduler)*

Start scheduled deployment.

Args:
    schedule_expr: Schedule expression (e.g., "daily", "*/6h", "3600")
    max_retries: Maximum retry attempts on failure
    
Returns:
    True if scheduler started successfully

## Signature

```python
def start(schedule_expr: str, max_retries: int) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `schedule_expr` | `str` | - |
| `max_retries` | `int` | - |

## Returns

Type: `bool`
