---
title: "Enable Security • AI Agents Framework"
sidebarTitle: "Enable Security"
description: "enable_security: Enable all security features in one call."
icon: "function"
---

# enable_security

<div className="flex items-center gap-2">
  <Badge color="teal">Function</Badge>
</div>

> This function is defined in the [**security**](../modules/security) module.

Enable all security features in one call.

Activates both injection defense and audit logging globally.

.. warning::
    These hooks register on the **process-global** ``praisonaiagents.hooks``
    registry and apply to every Agent in the process. This one-liner is not
    tenant-scoped: in a multi-tenant host (e.g. ``praisonai serve``) one
    tenant's ``extra_patterns`` would apply to all tenants. Keep the returned
    hook IDs and remove them via ``praisonaiagents.hooks.remove_hook`` to
    disable per feature.

## Signature

```python
def enable_security(log_path: Optional[str], include_output: bool, extra_patterns: Optional[List[str]]) -> dict
```

## Parameters

<ParamField query="log_path" type="Optional[str]" required={false}>
  Audit log path (default: ~/.praisonai/audit.jsonl).
</ParamField>

<ParamField query="include_output" type="bool" required={false} default="False">
  Include tool output in audit entries.
</ParamField>

<ParamField query="extra_patterns" type="Optional" required={false}>
  Extra injection detection patterns.
</ParamField>

### Returns

<ResponseField name="Returns" type="dict">
  &#123;"injection": str, "audit": str&#125;
</ResponseField>

## Usage

```python
>>> from praisonai.security import enable_security
    >>> enable_security()

    Then use Agent normally — no extra parameters needed:
    >>> from praisonaiagents import Agent
    >>> agent = Agent(instructions="You are a researcher")
    >>> agent.start("Research AI news")
```


## Uses

- `enable_injection_defense`
- `enable_audit_log`


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai/praisonai/security/__init__.py#L172">
  `praisonai/security/__init__.py` at line 172
</Card>
