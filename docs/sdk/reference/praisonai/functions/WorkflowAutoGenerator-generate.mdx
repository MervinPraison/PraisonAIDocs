---
title: "generate"
description: "API documentation for WorkflowAutoGenerator.generate"
icon: "function"
---

# WorkflowAutoGenerator.generate

*Class: [WorkflowAutoGenerator](../classes/WorkflowAutoGenerator)*

Generate a workflow YAML file.

Args:
    pattern: Workflow pattern - "sequential", "routing", "parallel", "loop",
             "orchestrator-workers", "evaluator-optimizer"
    merge: If True, merge with existing workflow file instead of overwriting
    
Returns:
    Path to the generated workflow file

## Signature

```python
def generate(pattern: str, merge: bool) -> str
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `pattern` | `str` | - |
| `merge` | `bool` | - |

## Returns

Type: `str`
