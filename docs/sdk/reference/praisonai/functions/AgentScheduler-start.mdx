---
title: "AgentScheduler.start"
description: "API documentation for AgentScheduler.start"
---

# AgentScheduler.start

*Class: [AgentScheduler](../classes/AgentScheduler)*

Start scheduled agent execution.

Args:
    schedule_expr: Schedule expression (e.g., "hourly", "*/1h", "3600")
    max_retries: Maximum retry attempts on failure
    run_immediately: If True, run agent immediately before starting schedule
    
Returns:
    True if scheduler started successfully

## Signature

```python
def start(schedule_expr: str, max_retries: int, run_immediately: bool) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `schedule_expr` | `str` | - |
| `max_retries` | `int` | - |
| `run_immediately` | `bool` | - |

## Returns

Type: `bool`
