---
title: "create_scheduler"
description: "API documentation for create_scheduler"
icon: "function"
---

# create_scheduler

*Module: [scheduler](../modules/scheduler)*

Factory function to create scheduler for different providers.

Args:
    provider: Deployment provider ("gcp", "aws", "azure", etc.)
    config: Optional configuration dict
    
Returns:
    Configured DeploymentScheduler instance

## Signature

```python
def create_scheduler(provider: str, config: Optional[Dict[str, Any]]) -> DeploymentScheduler
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `provider` | `str` | - |
| `config` | `Optional` | - |

## Returns

Type: `DeploymentScheduler`
