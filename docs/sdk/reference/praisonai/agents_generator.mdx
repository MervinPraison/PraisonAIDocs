---
title: "agents_generator"
description: "API reference for agents_generator"
icon: "wand"
---

# agents_generator

<Badge color="purple">Wrapper</Badge>

## Import

```python
from praisonai import agents_generator
```

## Classes

### AgentsGenerator

<Accordion title="Constructor Parameters">

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `agent_file` | `Any` | Yes | `-` |
| `framework` | `Any` | Yes | `-` |
| `config_list` | `Any` | Yes | `-` |
| `log_level` | `Any` | No | `None` |
| `agent_callback` | `Any` | No | `None` |
| `task_callback` | `Any` | No | `None` |
| `agent_yaml` | `Any` | No | `None` |
| `tools` | `Any` | No | `None` |

</Accordion>

<Accordion title="Methods">

- **is_function_or_decorated**(`obj: Any`) → `Any`
  Checks if the given object is a function or has a __call__ method.
- **load_tools_from_module**(`module_path: Any`) → `Any`
  Loads tools from a specified module path.
- **load_tools_from_module_class**(`module_path: Any`) → `Any`
  Loads tools from a specified module path containing classes that inherit from Ba
- **load_tools_from_package**(`package_path: Any`) → `Any`
  Loads tools from a specified package path containing modules with functions or c
- **load_tools_from_tools_py**(``) → `Any`
  Imports and returns all contents from tools.py file.
- **generate_crew_and_kickoff**(``) → `Any`
  Generates a crew of agents and initiates tasks based on the provided configurati

</Accordion>

## Functions

### safe_format()

Safely format a string template, preserving JSON-like curly braces.

This handles cases where templates contain Gutenberg block syntax like
&#123;"level":2&#125; which would cause KeyError with standard .format().

Uses a two-pass approach:
1. Escape all &#123;&#123; and &#125;&#125; (already escaped braces)
2. Only substitute known variable placeholders

```python
def safe_format(template: str) -> str
```

<Expandable title="Parameters">

- **template** (`str`)

</Expandable>

### noop()

```python
def noop() -> Any
```

### sanitize_agent_name_for_autogen_v4()

Sanitize agent name to be a valid Python identifier for AutoGen v0.4.

```python
def sanitize_agent_name_for_autogen_v4(name: Any) -> Any
```

<Expandable title="Parameters">

- **name** (`Any`)

</Expandable>

### disable_crewai_telemetry()

```python
def disable_crewai_telemetry() -> Any
```

## Constants

| Name | Value |
|------|-------|
| `CREWAI_AVAILABLE` | `False` |
| `AUTOGEN_AVAILABLE` | `False` |
| `AUTOGEN_V4_AVAILABLE` | `False` |
| `PRAISONAI_TOOLS_AVAILABLE` | `False` |
| `AGENTOPS_AVAILABLE` | `False` |
| `PRAISONAI_AVAILABLE` | `False` |
