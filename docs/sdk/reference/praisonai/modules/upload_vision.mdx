---
title: "upload_vision"
description: "This script handles uploading trained vision models to Hugging Face and Ollama."
---

# praisonai.upload_vision

<Badge color="purple">Wrapper</Badge>

This script handles uploading trained vision models to Hugging Face and Ollama.
It reads configuration from config.yaml and provides options to upload in different formats.

## Overview
This module provides components for upload_vision.

## Classes

**[Dedicated Page: UploadVisionModel](../classes/UploadVisionModel)**

### UploadVisionModel

<Accordion title="Constructor Parameters">

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `config_path` | `Any` | No | `'config.yaml'` |
</Accordion>

<Accordion title="Methods">
- **load_config**(`path: Any`) → `Any`
  Load configuration from yaml file.
- **prepare_model**(``) → `Any`
  Load the trained model for uploading.
- **save_model_merged**(``) → `Any`
  Save merged model to Hugging Face Hub.
- **push_model_gguf**(``) → `Any`
  Push model in GGUF format to Hugging Face Hub.
- **prepare_modelfile_content**(``) → `Any`
  Prepare Ollama modelfile content using Llama 3.2 vision template.
- **create_and_push_ollama_model**(``) → `Any`
  Create and push model to Ollama.
- **upload**(`target: Any`) → `Any`
  Upload the model to specified targets.
Args:
    target (str): One of 'all', 'hu
</Accordion>


## Functions

**[Dedicated Page: main()](../functions/main)**

### main()

```python
def main() -> Any
```

