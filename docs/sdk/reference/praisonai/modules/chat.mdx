---
title: "chat"
description: "PraisonAI Chat Module"
icon: "file-code"
---

# praisonai.chat

<Badge color="purple">Wrapper</Badge>

PraisonAI Chat Module

This module provides the chat UI integration for PraisonAI agents.
It uses the PraisonAI Chat (based on Chainlit) for the frontend.

## Overview
This module provides components for chat.

## Classes

**[Dedicated Page: ChatConfig](../classes/ChatConfig)**

### ChatConfig

Configuration for the PraisonAI Chat server.

<Accordion title="Constructor Parameters">

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `host` | `str` | No | `'0.0.0.0'` |
| `port` | `int` | No | `8000` |
| `debug` | `bool` | No | `False` |
| `auth_enabled` | `bool` | No | `False` |
| `session_id` | `Optional` | No | `None` |
</Accordion>


## Functions

**[Dedicated Page: start_chat_server()](../functions/start_chat_server)**

### start_chat_server()

Start the PraisonAI Chat server.

```python
def start_chat_server(agent: Optional[Any], agents: Optional[list], config: Optional[ChatConfig], port: int, host: str, debug: bool) -> None
```

<Expandable title="Parameters">
- **agent** (`Optional`)
  A single PraisonAI agent to use in the chat.
- **agents** (`Optional`)
  A list of PraisonAI agents for multi-agent chat.
- **config** (`Optional`)
  ChatConfig object with server settings.
- **port** (`int`)
  Port to run the server on (default: 8000).
- **host** (`str`)
  Host to bind to (default: 0.0.0.0).
- **debug** (`bool`)
  Enable debug mode (default: False).
</Expandable>


**[Dedicated Page: get_registered_agents()](../functions/get_registered_agents)**

### get_registered_agents()

Get all registered agents.

```python
def get_registered_agents() -> dict
```

