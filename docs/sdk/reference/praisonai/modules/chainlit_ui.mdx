---
title: "chainlit_ui"
description: "Module reference for chainlit_ui"
icon: "file-code"
---

# praisonai.chainlit_ui

<Badge color="purple">Wrapper</Badge>



## Overview
This module provides components for chainlit_ui.

## Functions

**[Dedicated Page: on_run()](../functions/on_run)**

### on_run()

```python
async def on_run(action: Any) -> Any
```

<Expandable title="Parameters">
- **action** (`Any`)
</Expandable>


**[Dedicated Page: on_modify()](../functions/on_modify)**

### on_modify()

```python
async def on_modify(action: Any) -> Any
```

<Expandable title="Parameters">
- **action** (`Any`)
</Expandable>


**[Dedicated Page: set_profiles()](../functions/set_profiles)**

### set_profiles()

```python
async def set_profiles(current_user: cl.User) -> Any
```

<Expandable title="Parameters">
- **current_user** (`cl.User`)
</Expandable>


**[Dedicated Page: start_chat()](../functions/start_chat)**

### start_chat()

```python
async def start_chat() -> Any
```


**[Dedicated Page: on_settings_update()](../functions/on_settings_update)**

### on_settings_update()

Handle updates to the ChatSettings form.

```python
async def on_settings_update(settings: Any) -> Any
```

<Expandable title="Parameters">
- **settings** (`Any`)
</Expandable>


**[Dedicated Page: on_chat_resume()](../functions/on_chat_resume)**

### on_chat_resume()

```python
async def on_chat_resume(thread: ThreadDict) -> Any
```

<Expandable title="Parameters">
- **thread** (`ThreadDict`)
</Expandable>


**[Dedicated Page: run_agents()](../functions/run_agents)**

### run_agents()

Runs the agents and returns the result.

```python
async def run_agents(agent_file: str, framework: str) -> Any
```

<Expandable title="Parameters">
- **agent_file** (`str`)
- **framework** (`str`)
</Expandable>


**[Dedicated Page: output()](../functions/output)**

### output()

```python
async def output(output: Any) -> Any
```

<Expandable title="Parameters">
- **output** (`Any`)
</Expandable>


**[Dedicated Page: agent()](../functions/agent)**

### agent()

```python
def agent(output: Any) -> Any
```

<Expandable title="Parameters">
- **output** (`Any`)
</Expandable>


**[Dedicated Page: task()](../functions/task)**

### task()

```python
def task(output: Any) -> Any
```

<Expandable title="Parameters">
- **output** (`Any`)
</Expandable>


**[Dedicated Page: main()](../functions/main)**

### main()

Run PraisonAI with the provided message as the topic.

```python
async def main(message: cl.Message) -> Any
```

<Expandable title="Parameters">
- **message** (`cl.Message`)
</Expandable>


**[Dedicated Page: auth_callback()](../functions/auth_callback)**

### auth_callback()

```python
def auth_callback(username: str, password: str) -> Any
```

<Expandable title="Parameters">
- **username** (`str`)
- **password** (`str`)
</Expandable>

