---
title: "knowledge"
description: "API reference for knowledge"
icon: "book-open"
---

# knowledge

<Badge color="blue">Core SDK</Badge>

## Import

```python
from praisonaiagents import knowledge
```

## Classes

### CustomMemory

### MongoDBMemory

MongoDB-based memory store for knowledge management.

<Accordion title="Constructor Parameters">

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `config` | `Any` | Yes | `-` |

</Accordion>

<Accordion title="Methods">

- **add**(`messages: Any, user_id: Any, agent_id: Any, run_id: Any, metadata: Any`) → `Any`
  Add memory to MongoDB.
- **search**(`query: Any, user_id: Any, agent_id: Any, run_id: Any, rerank: Any`) → `Any`
  Search memories in MongoDB.
- **get_all**(`user_id: Any, agent_id: Any, run_id: Any`) → `Any`
  Get all memories from MongoDB.
- **get**(`memory_id: Any`) → `Any`
  Get a specific memory by ID.
- **update**(`memory_id: Any, data: Any`) → `Any`
  Update a memory.
- **delete**(`memory_id: Any`) → `Any`
  Delete a memory.
- **delete_all**(`user_id: Any, agent_id: Any, run_id: Any`) → `Any`
  Delete all memories.
- **reset**(``) → `Any`
  Reset all memories.

</Accordion>

### Knowledge

<Accordion title="Constructor Parameters">

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `config` | `Any` | No | `None` |
| `verbose` | `Any` | No | `None` |

</Accordion>

<Accordion title="Methods">

- **config**(``) → `Any`
- **memory**(``) → `Any`
- **markdown**(``) → `Any`
- **chunker**(``) → `Any`
- **store**(`content: Any, user_id: Any, agent_id: Any, run_id: Any, metadata: Any`) → `Any`
  Store a memory.
- **get_all**(`user_id: Any, agent_id: Any, run_id: Any`) → `Any`
  Retrieve all memories.
- **get**(`memory_id: Any`) → `Any`
  Retrieve a specific memory by ID.
- **search**(`query: Any, user_id: Any, agent_id: Any, run_id: Any, rerank: Any`) → `Any`
  Search for memories related to a query.
- **update**(`memory_id: Any, data: Any`) → `Any`
  Update a memory.
- **history**(`memory_id: Any`) → `Any`
  Get the history of changes for a memory.
- **delete**(`memory_id: Any`) → `Any`
  Delete a memory.
- **delete_all**(`user_id: Any, agent_id: Any, run_id: Any`) → `Any`
  Delete all memories.
- **reset**(``) → `Any`
  Reset all memories.
- **normalize_content**(`content: Any`) → `Any`
  Normalize content for consistent storage.
- **add**(`file_path: Any, user_id: Any, agent_id: Any, run_id: Any, metadata: Any`) → `Any`
  Read file content and store it in memory.
- **index**(`path: str, incremental: bool, force: bool, include_glob: list, exclude_glob: list, user_id: str, agent_id: str, run_id: str`) → `Any`
  Index a directory or file for knowledge retrieval.
- **get_corpus_stats**(``) → `Any`
  Get statistics about the indexed corpus.

</Accordion>
