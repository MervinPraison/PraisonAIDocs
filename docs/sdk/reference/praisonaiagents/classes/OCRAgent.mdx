---
title: "OCRAgent"
description: "Class reference for OCRAgent"
---

# OCRAgent

*Module: [ocr_agent](../modules/ocr_agent)*

A specialized agent for OCR (Optical Character Recognition).

Extracts text from documents (PDFs) and images using AI models.

Supported Providers:
    - Mistral: `mistral/mistral-ocr-latest`

Example:
    ```python
    from praisonaiagents import OCRAgent
    
    agent = OCRAgent(llm="mistral/mistral-ocr-latest")
    
    # Extract from PDF URL
    result = agent.extract("https://example.com/document.pdf")
    print(result.text)
    
    # Extract from image URL
    result = agent.extract("https://example.com/image.png")
    for page in result.pages:
        print(page.markdown)
    ```

## Constructor

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `name` | `Optional` | No | `None` |
| `instructions` | `Optional` | No | `None` |
| `llm` | `Optional` | No | `None` |
| `model` | `Optional` | No | `None` |
| `base_url` | `Optional` | No | `None` |
| `api_key` | `Optional` | No | `None` |
| `ocr` | `Optional` | No | `None` |
| `verbose` | `Union` | No | `True` |

## Methods

- [console()](../functions/OCRAgent-console)
- [litellm()](../functions/OCRAgent-litellm)
- [extract()](../functions/OCRAgent-extract)
- [aextract()](../functions/OCRAgent-aextract)
- [read()](../functions/OCRAgent-read)
- [aread()](../functions/OCRAgent-aread)
