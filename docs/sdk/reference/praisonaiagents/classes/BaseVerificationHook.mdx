---
title: "BaseVerificationHook"
description: "Class reference for BaseVerificationHook"
icon: "brackets-curly"
---

# BaseVerificationHook

*Module: [verification](../modules/verification)*

Base class for verification hooks.

Provides common functionality for verification hooks.
Subclass this to create custom verification hooks.

Example:
    class MyTestRunner(BaseVerificationHook):
        name = "my_tests"
        
        def _execute(self, context):
            # Run your tests
            return VerificationResult(success=True, output="Tests passed")

## Constructor

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `name` | `Optional` | No | `None` |
| `timeout` | `float` | No | `60.0` |

## Properties

| Property | Type |
|----------|------|
| `name` | `str` |
| `timeout_seconds` | `float` |

## Methods

- [run()](../functions/BaseVerificationHook-run)
