---
title: "CachingConfig"
description: "Configuration for caching behavior."
icon: "brackets-curly"
---

# CachingConfig

> Defined in the [**feature_configs**](../modules/feature_configs) module.

Configuration for caching behavior.

Consolidates: cache, prompt_caching

## Properties

<ResponseField name="enabled" type="bool">
  No description available.
</ResponseField>

<ResponseField name="prompt_caching" type="Optional">
  No description available.
</ResponseField>

## Methods

<CardGroup cols={2}>
  <Card title="to_dict()" icon="function" href="../functions/CachingConfig-to_dict">
    Convert to dictionary.
  </Card>
</CardGroup>

## Usage

```python
# Simple enable
    Agent(caching=True)
    
    # With config
    Agent(caching=CachingConfig(
        enabled=True,
        prompt_caching=True,
    ))
```
