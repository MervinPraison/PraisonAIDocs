---
title: "is_middleware"
description: "API documentation for is_middleware"
icon: "function"
---

# is_middleware

*Module: [middleware](../modules/middleware)*

Check if a function is a middleware (wrap_* type).

## Signature

```python
def is_middleware(func: Callable) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `func` | `Callable` | - |

## Returns

Type: `bool`
