---
title: "Where Does It Run • AI Agent SDK"
sidebarTitle: "Where Does It Run"
description: "where_does_it_run: Plain-English answer to 'where does my code actually run?'."
icon: "function"
---

# where_does_it_run

<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**Agent**](../classes/Agent) class in the [**agent**](../modules/agent) module.

Plain-English answer to "where does my code actually run?".

Example::

```python
>>> print(Agent(name="builder").where_does_it_run())
Thinking (the AI model calls) happens on this machine.
Tools run on this machine.
Nothing is isolated: tools you pass run in this program, with your permissions.
```

## Signature

```python
def where_does_it_run() -> str
```

### Returns

<ResponseField name="Returns" type="str">
  The result of the operation.
</ResponseField>


## Uses

- `explain`


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai-agents/praisonaiagents/agent/agent.py#L7503">
  `praisonaiagents/agent/agent.py` at line 7503
</Card>
