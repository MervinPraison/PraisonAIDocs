---
title: "capture_llm_boundary"
description: "API documentation for ContextManager.capture_llm_boundary"
icon: "function"
---

# ContextManager.capture_llm_boundary

*Class: [ContextManager](../classes/ContextManager)*

Capture exact state at LLM call boundary.

Call this immediately before sending to LLM to get exact snapshot.

Args:
    messages: Exact messages being sent
    tools: Exact tool schemas being sent
    
Returns:
    SnapshotHookData with hashes for verification

## Signature

```python
def capture_llm_boundary(messages: List[Dict[str, Any]], tools: List[Dict[str, Any]]) -> SnapshotHookData
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `messages` | `List` | - |
| `tools` | `List` | - |

## Returns

Type: `SnapshotHookData`
