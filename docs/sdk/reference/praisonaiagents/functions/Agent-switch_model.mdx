---
title: "Agent.switch_model"
description: "API documentation for Agent.switch_model"
---

# Agent.switch_model

*Class: [Agent](../classes/Agent)*

Switch the agent's LLM model while preserving conversation history.

Args:
    new_model: The new model name to switch to (e.g., "gpt-4o", "claude-3-sonnet")

## Signature

```python
def switch_model(new_model: str) -> None
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `new_model` | `str` | - |

## Returns

Type: `None`
