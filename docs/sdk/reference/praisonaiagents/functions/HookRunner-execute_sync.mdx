---
title: "execute_sync"
description: "API documentation for HookRunner.execute_sync"
icon: "function"
---

# HookRunner.execute_sync

*Class: [HookRunner](../classes/HookRunner)*

Synchronous version of execute.

Args:
    event: The event to execute hooks for
    input_data: Input data for the hooks
    target: Optional target to filter hooks
    
Returns:
    List of execution results

## Signature

```python
def execute_sync(event: HookEvent, input_data: HookInput, target: Optional[str]) -> List[HookExecutionResult]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `event` | `HookEvent` | - |
| `input_data` | `HookInput` | - |
| `target` | `Optional` | - |

## Returns

Type: `List[HookExecutionResult]`
