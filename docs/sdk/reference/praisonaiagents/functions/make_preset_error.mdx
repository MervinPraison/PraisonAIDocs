---
title: "Make Preset Error • AI Agent"
description: "make_preset_error: Create a helpful error message for invalid preset."
icon: "function"
---

# make_preset_error

<div className="flex items-center gap-2">
  <Badge color="teal">Function</Badge>
</div>

> This function is defined in the [**parse_utils**](../modules/parse_utils) module.

Create a helpful error message for invalid preset.

## Signature

```python
def make_preset_error(param_name: str, value: str, presets: Iterable[str], url_schemes: Optional[Dict[str, str]]) -> ValueError
```

## Parameters

<ParamField query="param_name" type="str" required={true}>
  Name of the parameter
</ParamField>

<ParamField query="value" type="str" required={true}>
  Invalid value provided
</ParamField>

<ParamField query="presets" type="Iterable" required={true}>
  Valid preset options
</ParamField>

<ParamField query="url_schemes" type="Optional" required={false}>
  Optional URL schemes if applicable
</ParamField>

### Returns

<ResponseField name="Returns" type="ValueError">
  ValueError with helpful message
</ResponseField>
