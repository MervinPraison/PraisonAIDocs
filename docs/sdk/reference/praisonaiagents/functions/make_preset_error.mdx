---
title: "make_preset_error"
description: "API documentation for make_preset_error"
icon: "function"
---

# make_preset_error

*Module: [parse_utils](../modules/parse_utils)*

Create a helpful error message for invalid preset.

Args:
    param_name: Name of the parameter
    value: Invalid value provided
    presets: Valid preset options
    url_schemes: Optional URL schemes if applicable
    
Returns:
    ValueError with helpful message

## Signature

```python
def make_preset_error(param_name: str, value: str, presets: Iterable[str], url_schemes: Optional[Dict[str, str]]) -> ValueError
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `param_name` | `str` | - |
| `value` | `str` | - |
| `presets` | `Iterable` | - |
| `url_schemes` | `Optional` | - |

## Returns

Type: `ValueError`
