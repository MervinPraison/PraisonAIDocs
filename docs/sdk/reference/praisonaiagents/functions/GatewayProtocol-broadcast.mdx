---
title: "broadcast"
description: "Broadcast an event to all connected clients."
icon: "function"
---

# broadcast

<div className="flex items-center gap-2">
  <Badge color="blue">Async</Badge>
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**GatewayProtocol**](../classes/GatewayProtocol) class in the [**protocols**](../modules/protocols) module.

Broadcast an event to all connected clients.

## Signature

```python
async def broadcast(event: GatewayEvent, exclude: Optional[List[str]]) -> None
```

## Parameters

<ParamField query="event" type="GatewayEvent" required={true}>
  The event to broadcast
</ParamField>

<ParamField query="exclude" type="Optional" required={false}>
  Optional list of client IDs to exclude
</ParamField>
