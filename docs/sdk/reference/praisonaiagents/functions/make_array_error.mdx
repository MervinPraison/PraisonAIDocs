---
title: "make_array_error"
description: "API documentation for make_array_error"
icon: "function"
---

# make_array_error

*Module: [parse_utils](../modules/parse_utils)*

Create a helpful error message for invalid array format.

Args:
    param_name: Name of the parameter
    value: Invalid array value
    expected_format: Description of expected format
    
Returns:
    ValueError with helpful message

## Signature

```python
def make_array_error(param_name: str, value: list, expected_format: str) -> ValueError
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `param_name` | `str` | - |
| `value` | `list` | - |
| `expected_format` | `str` | - |

## Returns

Type: `ValueError`
