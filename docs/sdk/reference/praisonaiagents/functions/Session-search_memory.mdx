---
title: "Session.search_memory"
description: "API documentation for Session.search_memory"
---

# Session.search_memory

*Class: [Session](../classes/Session)*

Search session memory.

Args:
    query: Search query
    memory_type: "short" or "long" term memory
    limit: Maximum results to return

Returns:
    List of memory results

## Signature

```python
def search_memory(query: str, memory_type: str, limit: int) -> List[Dict[str, Any]]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `str` | - |
| `memory_type` | `str` | - |
| `limit` | `int` | - |

## Returns

Type: `List[Dict[str, Any]]`
