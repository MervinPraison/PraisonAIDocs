---
title: "execute"
description: "API documentation for AsyncMiddlewareChain.execute"
icon: "function"
---

# AsyncMiddlewareChain.execute

*Class: [AsyncMiddlewareChain](../classes/AsyncMiddlewareChain)*

Execute the async middleware chain.

Args:
    request: The request object to pass through
    final_handler: The actual async operation to perform
    
Returns:
    The result from the chain

## Signature

```python
async def execute(request: Any, final_handler: Callable) -> Any
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `request` | `Any` | - |
| `final_handler` | `Callable` | - |
