---
title: "execute"
description: "API documentation for MiddlewareChain.execute"
icon: "function"
---

# MiddlewareChain.execute

*Class: [MiddlewareChain](../classes/MiddlewareChain)*

Execute the middleware chain with a final handler.

Args:
    request: The request object to pass through
    final_handler: The actual operation to perform
    
Returns:
    The result from the chain

## Signature

```python
def execute(request: Any, final_handler: Callable[[Any], Any]) -> Any
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `request` | `Any` | - |
| `final_handler` | `Callable` | - |
