---
title: "parse_url_to_config"
description: "API documentation for parse_url_to_config"
icon: "function"
---

# parse_url_to_config

*Module: [parse_utils](../modules/parse_utils)*

Parse a URL string into a config object.

Args:
    url: URL string (e.g., "postgresql://localhost/db")
    config_class: Config dataclass to instantiate
    url_schemes: Mapping of URL schemes to backend names
    
Returns:
    Config instance with backend and url set
    
Raises:
    ValueError: If URL scheme is not supported

## Signature

```python
def parse_url_to_config(url: str, config_class: type, url_schemes: Dict[str, str]) -> Any
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `url` | `str` | - |
| `config_class` | `type` | - |
| `url_schemes` | `Dict` | - |
