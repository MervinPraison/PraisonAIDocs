---
title: "Jittered Backoff • AI Agent SDK"
sidebarTitle: "Jittered Backoff"
description: "jittered_backoff: Calculate delay for jittered exponential backoff."
icon: "function"
---

# jittered_backoff

<div className="flex items-center gap-2">
  <Badge color="teal">Function</Badge>
</div>

> This function is defined in the [**retry_utils**](../modules/retry_utils) module.

Calculate delay for jittered exponential backoff.

## Signature

```python
def jittered_backoff(attempt: int) -> float
```

## Parameters

<ParamField query="attempt" type="int" required={true}>
  Current attempt number (0-based)
</ParamField>

### Returns

<ResponseField name="Returns" type="float">
  Delay in seconds with jitter applied
</ResponseField>

## Usage

```python
>>> # Attempt 0: ~5s, Attempt 1: ~10s, Attempt 2: ~20s
    >>> delay = jittered_backoff(1, base_delay=5.0, max_delay=120.0, jitter_ratio=0.5)
```


## Uses

- `random.uniform`


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai-agents/praisonaiagents/agent/retry_utils.py#L35">
  `praisonaiagents/agent/retry_utils.py` at line 35
</Card>
