---
title: "prune_history"
description: "API documentation for Agent.prune_history"
icon: "function"
---

# Agent.prune_history

*Class: [Agent](../classes/Agent)*

Prune chat history to keep only the last N messages.

Useful for cleaning up large history after image analysis sessions
to prevent context window saturation.

Args:
    keep_last: Number of recent messages to keep
    
Returns:
    Number of messages deleted

## Signature

```python
def prune_history(keep_last: int) -> int
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `keep_last` | `int` | - |

## Returns

Type: `int`
