---
title: "search_user_memory"
description: "API documentation for Memory.search_user_memory"
icon: "function"
---

# Memory.search_user_memory

*Class: [Memory](../classes/Memory)*

If mem0 is used, pass user_id in. Otherwise fallback to local filter on user in metadata.

## Signature

```python
def search_user_memory(user_id: str, query: str, limit: int, rerank: bool) -> List[Dict[str, Any]]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `user_id` | `str` | - |
| `query` | `str` | - |
| `limit` | `int` | - |
| `rerank` | `bool` | - |

## Returns

Type: `List[Dict[str, Any]]`
