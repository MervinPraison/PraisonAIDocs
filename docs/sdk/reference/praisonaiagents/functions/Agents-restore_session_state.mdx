---
title: "restore_session_state"
description: "Restore state from memory for session persistence. Returns True if restored."
icon: "function"
---

# restore_session_state

<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**Agents**](../classes/Agents) class in the [**agents**](../modules/agents) module.

Restore state from memory for session persistence. Returns True if restored.

## Signature

```python
def restore_session_state(session_id: str) -> bool
```

## Parameters

<ParamField query="session_id" type="str" required={true}>
  No description available.
</ParamField>

### Returns

<ResponseField name="Returns" type="bool">
  The result of the operation.
</ResponseField>
