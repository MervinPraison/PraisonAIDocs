---
title: "Memory.delete_memories_matching"
description: "API documentation for Memory.delete_memories_matching"
---

# Memory.delete_memories_matching

*Class: [Memory](../classes/Memory)*

Delete memories matching a search query.

Useful for bulk cleanup of related memories, e.g., all image-related
context after finishing an image analysis session.

Args:
    query: Search query to match memories
    memory_type: Optional type ('short_term' or 'long_term')
    limit: Maximum number of memories to delete
    
Returns:
    Number of memories deleted

## Signature

```python
def delete_memories_matching(query: str, memory_type: Optional[str], limit: int) -> int
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `str` | - |
| `memory_type` | `Optional` | - |
| `limit` | `int` | - |

## Returns

Type: `int`
