---
title: "ContextManager.process"
description: "API documentation for ContextManager.process"
---

# ContextManager.process

*Class: [ContextManager](../classes/ContextManager)*

Process messages through the context pipeline.

Applies budgeting, optimization, and monitoring.

Args:
    messages: Conversation messages
    system_prompt: System prompt content
    tools: Tool schemas
    trigger: What triggered this processing
    
Returns:
    Dict with processed messages and metadata

## Signature

```python
def process(messages: List[Dict[str, Any]], system_prompt: str, tools: Optional[List[Dict[str, Any]]], trigger: Literal['turn', 'tool_call', 'manual', 'overflow']) -> Dict[str, Any]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `messages` | `List` | - |
| `system_prompt` | `str` | - |
| `tools` | `Optional` | - |
| `trigger` | `Literal` | - |

## Returns

Type: `Dict[str, Any]`
