---
title: "MultiAgentContextManager.prepare_handoff"
description: "API documentation for MultiAgentContextManager.prepare_handoff"
---

# MultiAgentContextManager.prepare_handoff

*Class: [MultiAgentContextManager](../classes/MultiAgentContextManager)*

Prepare context for handoff between agents.

Args:
    from_agent: Source agent ID
    to_agent: Target agent ID
    messages: Current messages
    policy: Override policy for this handoff
    
Returns:
    Messages to pass to target agent

## Signature

```python
def prepare_handoff(from_agent: str, to_agent: str, messages: List[Dict[str, Any]], policy: Optional[ContextPolicy]) -> List[Dict[str, Any]]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `from_agent` | `str` | - |
| `to_agent` | `str` | - |
| `messages` | `List` | - |
| `policy` | `Optional` | - |

## Returns

Type: `List[Dict[str, Any]]`
