---
title: "Agent.astart"
description: "API documentation for Agent.astart"
---

# Agent.astart

*Class: [Agent](../classes/Agent)*

Async version of start() - interactive, streaming-aware.

Beginner-friendly async execution. Streams by default when in TTY.

Args:
    prompt: The input prompt to process
    **kwargs: Additional arguments passed to achat()
    
Returns:
    The agent's response as a string

## Signature

```python
async def astart(prompt: str) -> Any
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `prompt` | `str` | - |
