---
title: "Merge Config With Overrides | AI Agent"
description: "merge_config_with_overrides: Merge a base config with override dict."
icon: "function"
---

# merge_config_with_overrides

<div className="flex items-center gap-2">
  <Badge color="teal">Function</Badge>
</div>

> This function is defined in the [**parse_utils**](../modules/parse_utils) module.

Merge a base config with override dict.

## Signature

```python
def merge_config_with_overrides(base_config: Any, overrides: Dict[str, Any], config_class: type) -> Any
```

## Parameters

<ParamField query="base_config" type="Any" required={true}>
  Base config instance
</ParamField>

<ParamField query="overrides" type="Dict" required={true}>
  Dict of field overrides
</ParamField>

<ParamField query="config_class" type="type" required={true}>
  Config class for creating new instance
</ParamField>

### Returns

<ResponseField name="Returns" type="Any">
  New config instance with overrides applied
</ResponseField>
