---
title: "merge_config_with_overrides"
description: "API documentation for merge_config_with_overrides"
icon: "function"
---

# merge_config_with_overrides

*Module: [parse_utils](../modules/parse_utils)*

Merge a base config with override dict.

Args:
    base_config: Base config instance
    overrides: Dict of field overrides
    config_class: Config class for creating new instance
    
Returns:
    New config instance with overrides applied

## Signature

```python
def merge_config_with_overrides(base_config: Any, overrides: Dict[str, Any], config_class: type) -> Any
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `base_config` | `Any` | - |
| `overrides` | `Dict` | - |
| `config_class` | `type` | - |
