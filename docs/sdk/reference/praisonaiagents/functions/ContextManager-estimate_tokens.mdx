---
title: "estimate_tokens"
description: "API documentation for ContextManager.estimate_tokens"
icon: "function"
---

# ContextManager.estimate_tokens

*Class: [ContextManager](../classes/ContextManager)*

Estimate tokens with optional validation.

Args:
    text: Text to estimate
    validate: Whether to validate against accurate count
    
Returns:
    Tuple of (token_count, metrics)

## Signature

```python
def estimate_tokens(text: str, validate: bool) -> Tuple[int, Optional[EstimationMetrics]]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `text` | `str` | - |
| `validate` | `bool` | - |

## Returns

Type: `Tuple[int, Optional[EstimationMetrics]]`
