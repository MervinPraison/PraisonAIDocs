---
title: "is_path_like"
description: "API documentation for is_path_like"
---

# is_path_like

*Module: [parse_utils](../modules/parse_utils)*

Check if a string looks like a file path. O(1) operation.

Args:
    value: String to check
    
Returns:
    True if string looks like a path
    
Examples:
    &gt;&gt;&gt; is_path_like("docs/")
    True
    &gt;&gt;&gt; is_path_like("./data.pdf")
    True
    &gt;&gt;&gt; is_path_like("verbose")
    False

## Signature

```python
def is_path_like(value: str) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `value` | `str` | - |

## Returns

Type: `bool`
