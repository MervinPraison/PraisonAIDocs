---
title: "Memory.build_context_for_task"
description: "API documentation for Memory.build_context_for_task"
---

# Memory.build_context_for_task

*Class: [Memory](../classes/Memory)*

Merges relevant short-term, long-term, entity, user memories
into a single text block with deduplication and clean formatting.

Args:
    include_in_output: If None, memory content is only included when debug logging is enabled.
                       If True, memory content is always included.
                       If False, memory content is never included (only logged for debugging).

## Signature

```python
def build_context_for_task(task_descr: str, user_id: Optional[str], additional: str, max_items: int, include_in_output: Optional[bool]) -> str
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `task_descr` | `str` | - |
| `user_id` | `Optional` | - |
| `additional` | `str` | - |
| `max_items` | `int` | - |
| `include_in_output` | `Optional` | - |

## Returns

Type: `str`
