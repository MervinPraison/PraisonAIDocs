---
title: "VideoAgent.wait_for_completion"
description: "API documentation for VideoAgent.wait_for_completion"
---

# VideoAgent.wait_for_completion

*Class: [VideoAgent](../classes/VideoAgent)*

Wait for video generation to complete.

Args:
    video_id: The video ID to poll
    poll_interval: Seconds between status checks (default: 10)
    max_wait_time: Maximum wait time in seconds (default: 600)
    
Returns:
    VideoObject with status "completed" or "failed"
    
Raises:
    TimeoutError: If max_wait_time is exceeded

## Signature

```python
def wait_for_completion(video_id: str, poll_interval: Optional[int], max_wait_time: Optional[int]) -> Any
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `video_id` | `str` | - |
| `poll_interval` | `Optional` | - |
| `max_wait_time` | `Optional` | - |
