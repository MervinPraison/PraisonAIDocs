---
title: "ContextAgent.execute_prp"
description: "API documentation for ContextAgent.execute_prp"
---

# ContextAgent.execute_prp

*Class: [ContextAgent](../classes/ContextAgent)*

Execute a PRP following PRD methodology (placeholder for future implementation).

## Signature

```python
def execute_prp(prp_file_path: str) -> Dict[str, Any]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `prp_file_path` | `str` | - |

## Returns

Type: `Dict[str, Any]`
