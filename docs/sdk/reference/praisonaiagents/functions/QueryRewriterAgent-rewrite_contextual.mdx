---
title: "QueryRewriterAgent.rewrite_contextual"
description: "API documentation for QueryRewriterAgent.rewrite_contextual"
---

# QueryRewriterAgent.rewrite_contextual

*Class: [QueryRewriterAgent](../classes/QueryRewriterAgent)*

Convenience method for contextual rewriting.

## Signature

```python
def rewrite_contextual(query: str, chat_history: List[Dict[str, str]]) -> RewriteResult
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `str` | - |
| `chat_history` | `List` | - |

## Returns

Type: `RewriteResult`
