---
title: "From Metadata • AI Agent SDK"
sidebarTitle: "From Metadata"
description: "from_metadata: Parse an automation block from a skill's ``metadata`` mapping."
icon: "function"
---

# from_metadata

<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**SkillAutomation**](../classes/SkillAutomation) class in the [**models**](../modules/models) module.

Parse an automation block from a skill's ``metadata`` mapping.

Looks up ``metadata['praisonai']['automation']``. Returns None when the
block is absent or malformed (missing/empty ``schedule``), so skills
without automation simply parse to ``automation=None``.

## Signature

```python
def from_metadata(metadata: dict) -> Optional['SkillAutomation']
```

## Parameters

<ParamField query="metadata" type="dict" required={true}>
  The parsed ``metadata`` bag from SKILL.md frontmatter.
</ParamField>

### Returns

<ResponseField name="Returns" type="Optional['SkillAutomation']">
  SkillAutomation instance, or None when no valid block is present.
</ResponseField>


## Uses

- `cls`


## Used By

- [`read_properties`](../functions/read_properties)


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai-agents/praisonaiagents/skills/models.py#L170">
  `praisonaiagents/skills/models.py` at line 170
</Card>
