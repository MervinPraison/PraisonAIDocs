---
title: "Agents.update_plan_step_status"
description: "API documentation for Agents.update_plan_step_status"
---

# Agents.update_plan_step_status

*Class: [Agents](../classes/Agents)*

Update the status of a plan step.

Args:
    step_id: ID of the step to update
    status: New status
    
Returns:
    True if updated, False if not found

## Signature

```python
def update_plan_step_status(step_id: str, status: str) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `step_id` | `str` | - |
| `status` | `str` | - |

## Returns

Type: `bool`
