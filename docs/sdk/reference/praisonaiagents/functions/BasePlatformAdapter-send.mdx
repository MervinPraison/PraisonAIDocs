---
title: "send • AI Agent SDK"
sidebarTitle: "send"
description: "send: Send a single message to *chat_id* (one API call, no chunking)."
icon: "function"
---

# send

<div className="flex items-center gap-2">
  <Badge color="blue">Async</Badge>
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**BasePlatformAdapter**](../classes/BasePlatformAdapter) class in the [**base**](../modules/base) module.

Send a single message to *chat_id* (one API call, no chunking).

This is the low-level primitive the default delivery machinery builds
on. Implementations should send *content* as-is and return a
:class:`SendResult`. Chunking/retry/typing are handled by
:meth:`deliver`; implementers need not repeat them here.

## Signature

```python
async def send(chat_id: Any, content: Union[str, Dict[str, Any]]) -> SendResult
```

## Parameters

<ParamField query="chat_id" type="Any" required={true}>
  No description available.
</ParamField>

<ParamField query="content" type="Union" required={true}>
  No description available.
</ParamField>

### Returns

<ResponseField name="Returns" type="SendResult">
  The result of the operation.
</ResponseField>


## Used By

- [`AgentTeam.run_all_tasks`](../functions/AgentTeam-run_all_tasks)


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai-agents/praisonaiagents/bots/base.py#L385">
  `praisonaiagents/bots/base.py` at line 385
</Card>
