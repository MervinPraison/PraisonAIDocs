---
title: "get_context"
description: "API documentation for Session.get_context"
icon: "function"
---

# Session.get_context

*Class: [Session](../classes/Session)*

Build context from session memory and knowledge.

Args:
    query: Query to build context for
    max_items: Maximum items per section

Returns:
    Formatted context string

## Signature

```python
def get_context(query: str, max_items: int) -> str
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `str` | - |
| `max_items` | `int` | - |

## Returns

Type: `str`
