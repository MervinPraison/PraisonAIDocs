---
title: "get_hooks"
description: "API documentation for HookRegistry.get_hooks"
icon: "function"
---

# HookRegistry.get_hooks

*Class: [HookRegistry](../classes/HookRegistry)*

Get all hooks for an event, optionally filtered by target.

Args:
    event: The event to get hooks for
    target: Optional target to filter by (e.g., tool name)
    
Returns:
    List of matching hook definitions

## Signature

```python
def get_hooks(event: HookEvent, target: Optional[str]) -> List[HookDefinition]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `event` | `HookEvent` | - |
| `target` | `Optional` | - |

## Returns

Type: `List[HookDefinition]`
