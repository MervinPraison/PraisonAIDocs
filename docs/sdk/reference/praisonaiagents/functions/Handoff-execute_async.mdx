---
title: "execute_async"
description: "API documentation for Handoff.execute_async"
icon: "function"
---

# Handoff.execute_async

*Class: [Handoff](../classes/Handoff)*

Execute handoff asynchronously with concurrency control.

Args:
    source_agent: The agent initiating the handoff
    prompt: The task/prompt to pass to target agent
    context: Optional additional context
    
Returns:
    HandoffResult with response or error

## Signature

```python
async def execute_async(source_agent: 'Agent', prompt: str, context: Optional[Dict[str, Any]]) -> HandoffResult
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `source_agent` | `Agent` | - |
| `prompt` | `str` | - |
| `context` | `Optional` | - |

## Returns

Type: `HandoffResult`
