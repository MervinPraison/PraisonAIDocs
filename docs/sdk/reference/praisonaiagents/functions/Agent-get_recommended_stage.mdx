---
title: "Agent.get_recommended_stage"
description: "API documentation for Agent.get_recommended_stage"
---

# Agent.get_recommended_stage

*Class: [Agent](../classes/Agent)*

Get recommended execution stage for prompt.

Args:
    prompt: The user prompt
    
Returns:
    Stage name as string (direct, heuristic, planned, autonomous)

## Signature

```python
def get_recommended_stage(prompt: str) -> str
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `prompt` | `str` | - |

## Returns

Type: `str`
