---
title: "unregister"
description: "API documentation for HookRegistry.unregister"
icon: "function"
---

# HookRegistry.unregister

*Class: [HookRegistry](../classes/HookRegistry)*

Unregister a hook by ID.

Args:
    hook_id: The hook ID to unregister
    
Returns:
    True if found and removed, False otherwise

## Signature

```python
def unregister(hook_id: str) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `hook_id` | `str` | - |

## Returns

Type: `bool`
