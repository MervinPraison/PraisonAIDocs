---
title: "launch"
description: "API documentation for Agent.launch"
icon: "function"
---

# Agent.launch

*Class: [Agent](../classes/Agent)*

Launch the agent as an HTTP API endpoint or an MCP server.

Args:
    path: API endpoint path (default: '/') for HTTP, or base path for MCP.
    port: Server port (default: 8000)
    host: Server host (default: '0.0.0.0')
    debug: Enable debug mode for uvicorn (default: False)
    protocol: "http" to launch as FastAPI, "mcp" to launch as MCP server.
    
Returns:
    None

## Signature

```python
def launch(path: str, port: int, host: str, debug: bool, protocol: str) -> Any
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `path` | `str` | - |
| `port` | `int` | - |
| `host` | `str` | - |
| `debug` | `bool` | - |
| `protocol` | `str` | - |
