---
title: "get_rules_context"
description: "API documentation for Agent.get_rules_context"
icon: "function"
---

# Agent.get_rules_context

*Class: [Agent](../classes/Agent)*

Get rules context for the current conversation.

Args:
    file_path: Optional file path for glob-based rule matching
    include_manual: Optional list of manual rule names to include (via @mention)
    
Returns:
    Formatted rules context string

## Signature

```python
def get_rules_context(file_path: Optional[str], include_manual: Optional[List[str]]) -> str
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `file_path` | `Optional` | - |
| `include_manual` | `Optional` | - |

## Returns

Type: `str`
