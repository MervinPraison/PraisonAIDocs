---
title: "download"
description: "API documentation for VideoAgent.download"
icon: "function"
---

# VideoAgent.download

*Class: [VideoAgent](../classes/VideoAgent)*

Download a video to a file.

Args:
    video_id: The video ID to download
    output: Path to save the video file
    
Returns:
    Path to the saved file

## Signature

```python
def download(video_id: str, output: str) -> str
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `video_id` | `str` | - |
| `output` | `str` | - |

## Returns

Type: `str`
