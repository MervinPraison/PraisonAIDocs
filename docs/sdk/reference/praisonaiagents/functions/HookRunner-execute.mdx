---
title: "execute"
description: "API documentation for HookRunner.execute"
icon: "function"
---

# HookRunner.execute

*Class: [HookRunner](../classes/HookRunner)*

Execute all hooks for an event.

Args:
    event: The event to execute hooks for
    input_data: Input data for the hooks
    target: Optional target to filter hooks (e.g., tool name)
    
Returns:
    List of execution results

## Signature

```python
async def execute(event: HookEvent, input_data: HookInput, target: Optional[str]) -> List[HookExecutionResult]
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `event` | `HookEvent` | - |
| `input_data` | `HookInput` | - |
| `target` | `Optional` | - |

## Returns

Type: `List[HookExecutionResult]`
