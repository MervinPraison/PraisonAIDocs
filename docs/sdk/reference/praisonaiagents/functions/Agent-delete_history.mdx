---
title: "delete_history"
description: "API documentation for Agent.delete_history"
icon: "function"
---

# Agent.delete_history

*Class: [Agent](../classes/Agent)*

Delete a specific message from chat history by index.

Supports negative indexing (-1 for last message, etc.).

Args:
    index: Message index (0-based, supports negative indexing)
    
Returns:
    True if deleted, False if index out of range

## Signature

```python
def delete_history(index: int) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `index` | `int` | - |

## Returns

Type: `bool`
