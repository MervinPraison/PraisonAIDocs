---
title: "approve • AI Agent SDK"
sidebarTitle: "approve"
description: "approve: Approve a pairing code, authorizing the channel."
icon: "function"
---

# approve

<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**PairingProtocol**](../classes/PairingProtocol) class in the [**protocols**](../modules/protocols) module.

Approve a pairing code, authorizing the channel.

## Signature

```python
def approve(channel_type: str, code: str, user_id: str, user_name: str) -> bool
```

## Parameters

<ParamField query="channel_type" type="str" required={true}>
  Type of channel
</ParamField>

<ParamField query="code" type="str" required={true}>
  The pairing code to approve
</ParamField>

<ParamField query="user_id" type="str" required={false} default="''">
  User identifier (optional, defaults to empty string)
</ParamField>

<ParamField query="user_name" type="str" required={false} default="''">
  Human-readable username (optional, defaults to empty string)
</ParamField>

### Returns

<ResponseField name="Returns" type="bool">
  True if approval successful, False if code invalid/expired
</ResponseField>


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai-agents/praisonaiagents/gateway/protocols.py#L2429">
  `praisonaiagents/gateway/protocols.py` at line 2429
</Card>
