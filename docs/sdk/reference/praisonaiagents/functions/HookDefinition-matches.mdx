---
title: "matches • AI Agent SDK"
sidebarTitle: "matches"
description: "matches: Check if this hook matches the target (tool name, etc.)."
icon: "function"
---

# matches

<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**HookDefinition**](../classes/HookDefinition) class in the [**types**](../modules/types) module.

Check if this hook matches the target (tool name, etc.).

## Signature

```python
def matches(target: str) -> bool
```

## Parameters

<ParamField query="target" type="str" required={true}>
  No description available.
</ParamField>

### Returns

<ResponseField name="Returns" type="bool">
  The result of the operation.
</ResponseField>
