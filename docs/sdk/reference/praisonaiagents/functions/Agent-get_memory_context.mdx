---
title: "get_memory_context"
description: "Get memory context for the current conversation."
icon: "function"
---

# get_memory_context

<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**Agent**](../classes/Agent) class in the [**agent**](../modules/agent) module.

Get memory context for the current conversation.

## Signature

```python
def get_memory_context(query: Optional[str]) -> str
```

## Parameters

<ParamField query="query" type="Optional" required={false}>
  Optional query to focus the context
</ParamField>

### Returns

<ResponseField name="Returns" type="str">
  Formatted memory context string
</ResponseField>
