---
title: "get_memory_context"
description: "API documentation for Agent.get_memory_context"
icon: "function"
---

# Agent.get_memory_context

*Class: [Agent](../classes/Agent)*

Get memory context for the current conversation.

Args:
    query: Optional query to focus the context
    
Returns:
    Formatted memory context string

## Signature

```python
def get_memory_context(query: Optional[str]) -> str
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `Optional` | - |

## Returns

Type: `str`
