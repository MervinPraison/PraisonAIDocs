---
title: "delete_state"
description: "Delete a state key if it exists. Returns True if deleted, False if key didn't exist."
icon: "function"
---


<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**Agents**](../classes/Agents) class in the [**agents**](../modules/agents) module.

Delete a state key if it exists. Returns True if deleted, False if key didn't exist.

## Signature

```python
def delete_state(key: str) -> bool
```

## Parameters

<ParamField query="key" type="str" required={true}>
  No description available.
</ParamField>

### Returns

<ResponseField name="Returns" type="bool">
  The result of the operation.
</ResponseField>
