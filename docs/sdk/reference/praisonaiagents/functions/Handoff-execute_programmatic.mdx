---
title: "Handoff.execute_programmatic"
description: "API documentation for Handoff.execute_programmatic"
---

# Handoff.execute_programmatic

*Class: [Handoff](../classes/Handoff)*

Execute handoff programmatically (not via LLM tool call).

This is the unified programmatic handoff API that replaces Agent.delegate().

Args:
    source_agent: The agent initiating the handoff
    prompt: The task/prompt to pass to target agent
    context: Optional additional context
    
Returns:
    HandoffResult with response or error

## Signature

```python
def execute_programmatic(source_agent: 'Agent', prompt: str, context: Optional[Dict[str, Any]]) -> HandoffResult
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `source_agent` | `Agent` | - |
| `prompt` | `str` | - |
| `context` | `Optional` | - |

## Returns

Type: `HandoffResult`
