---
title: "check_and_add"
description: "API documentation for SessionDeduplicationCache.check_and_add"
icon: "function"
---

# SessionDeduplicationCache.check_and_add

*Class: [SessionDeduplicationCache](../classes/SessionDeduplicationCache)*

Check if content hash exists and add if new.

Args:
    content_hash: Hash of the content
    agent_name: Name of the agent adding this content
    tokens: Estimated tokens in this content
    
Returns:
    True if duplicate (already exists), False if new

## Signature

```python
def check_and_add(content_hash: str, agent_name: str, tokens: int) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `content_hash` | `str` | - |
| `agent_name` | `str` | - |
| `tokens` | `int` | - |

## Returns

Type: `bool`
