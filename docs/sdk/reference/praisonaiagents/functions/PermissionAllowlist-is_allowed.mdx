---
title: "PermissionAllowlist.is_allowed"
description: "API documentation for PermissionAllowlist.is_allowed"
---

# PermissionAllowlist.is_allowed

*Class: [PermissionAllowlist](../classes/PermissionAllowlist)*

Check if a tool is allowed.

Args:
    tool_name: Name of the tool
    path: Optional path to check against allowed paths
    
Returns:
    True if tool is allowed (optionally for the given path)

## Signature

```python
def is_allowed(tool_name: str, path: Optional[str]) -> bool
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `tool_name` | `str` | - |
| `path` | `Optional` | - |

## Returns

Type: `bool`
