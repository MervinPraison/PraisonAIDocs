---
title: "execute_tool_call"
description: "API documentation for MiddlewareManager.execute_tool_call"
icon: "function"
---

# MiddlewareManager.execute_tool_call

*Class: [MiddlewareManager](../classes/MiddlewareManager)*

Execute a tool call with all hooks and middleware.

Order:
1. before_tool hooks (in order)
2. wrap_tool_call middleware chain
3. actual tool call
4. after_tool hooks (in reverse order)

## Signature

```python
def execute_tool_call(request: ToolRequest, tool_fn: Callable[[ToolRequest], ToolResponse]) -> ToolResponse
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `request` | `ToolRequest` | - |
| `tool_fn` | `Callable` | - |

## Returns

Type: `ToolResponse`
