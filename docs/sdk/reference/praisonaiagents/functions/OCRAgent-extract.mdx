---
title: "extract"
description: "Extract text from a document or image."
icon: "function"
---


<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**OCRAgent**](../classes/OCRAgent) class in the [**ocr_agent**](../modules/ocr_agent) module.

Extract text from a document or image.

## Signature

```python
def extract(source: str, include_image_base64: Optional[bool], pages: Optional[List[int]], image_limit: Optional[int], model: Optional[str]) -> Any
```

## Parameters

<ParamField query="source" type="str" required={true}>
  URL or path to document/image
</ParamField>

<ParamField query="include_image_base64" type="Optional" required={false}>
  Include base64 images in response
</ParamField>

<ParamField query="pages" type="Optional" required={false}>
  Specific pages to extract (for PDFs)
</ParamField>

<ParamField query="image_limit" type="Optional" required={false}>
  Maximum images per page
</ParamField>

<ParamField query="model" type="Optional" required={false}>
  Override model for this call **kwargs: Additional parameters
</ParamField>

### Returns

<ResponseField name="Returns" type="Any">
  OCRResponse with pages, markdown content, and metadata
</ResponseField>

## Usage

```python
agent = OCRAgent(llm="mistral/mistral-ocr-latest")
    result = agent.extract("https://arxiv.org/pdf/2201.04234")
    for page in result.pages:
        print(f"Page {page.index}: {page.markdown}")
```
