---
title: "Handoff.to_tool_function"
description: "API documentation for Handoff.to_tool_function"
---

# Handoff.to_tool_function

*Class: [Handoff](../classes/Handoff)*

Convert this handoff to a tool function that can be called by the LLM.

Args:
    source_agent: The agent that will be using this handoff
    
Returns:
    A callable function that performs the handoff

## Signature

```python
def to_tool_function(source_agent: 'Agent') -> Callable
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `source_agent` | `Agent` | - |

## Returns

Type: `Callable`
