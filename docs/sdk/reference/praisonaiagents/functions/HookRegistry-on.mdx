---
title: "on"
description: "API documentation for HookRegistry.on"
icon: "function"
---

# HookRegistry.on

*Class: [HookRegistry](../classes/HookRegistry)*

Decorator to register a function as a hook.

Args:
    event: The event to hook
    matcher: Optional regex pattern to match targets
    sequential: Whether to run sequentially
    timeout: Timeout in seconds
    
Returns:
    Decorator function
    
Example:
    @registry.on(HookEvent.BEFORE_TOOL)
    def my_hook(event_data):
        return HookResult.allow()

## Signature

```python
def on(event: HookEvent, matcher: Optional[str], sequential: bool, timeout: float) -> Callable
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `event` | `HookEvent` | - |
| `matcher` | `Optional` | - |
| `sequential` | `bool` | - |
| `timeout` | `float` | - |

## Returns

Type: `Callable`
