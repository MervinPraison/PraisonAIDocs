---
title: "AudioAgent.speech"
description: "API documentation for AudioAgent.speech"
---

# AudioAgent.speech

*Class: [AudioAgent](../classes/AudioAgent)*

Convert text to speech.

Args:
    text: Text to convert to speech
    output: Path to save audio file (optional)
    voice: Voice to use (e.g., "alloy", "echo", "fable")
    speed: Speech speed (0.25 to 4.0)
    response_format: Audio format (mp3, opus, aac, flac, wav)
    model: Override model for this call
    **kwargs: Additional provider-specific parameters
    
Returns:
    Audio response object with stream_to_file() method
    
Example:
    ```python
    agent = AudioAgent(llm="openai/tts-1")
    agent.speech("Hello world!", output="hello.mp3")
    ```

## Signature

```python
def speech(text: str, output: Optional[str], voice: Optional[str], speed: Optional[float], response_format: Optional[str], model: Optional[str]) -> Any
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `text` | `str` | - |
| `output` | `Optional` | - |
| `voice` | `Optional` | - |
| `speed` | `Optional` | - |
| `response_format` | `Optional` | - |
| `model` | `Optional` | - |
