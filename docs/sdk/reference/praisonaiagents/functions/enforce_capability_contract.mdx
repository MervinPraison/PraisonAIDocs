---
title: "Enforce Capability Contract • AI Agent SDK"
sidebarTitle: "Enforce Capability Contract"
description: "enforce_capability_contract: Verify *adapter*'s capability contract and raise on any violation."
icon: "function"
---

# enforce_capability_contract

<div className="flex items-center gap-2">
  <Badge color="teal">Function</Badge>
</div>

> This function is defined in the [**base**](../modules/base) module.

Verify *adapter*'s capability contract and raise on any violation.

Thin wrapper over :func:`verify_capability_contract` that raises a single
:class:`CapabilityContractError` naming every declared-but-unbacked
capability, so an adapter cannot advertise something it cannot do. Intended
to be called once where adapters are built/registered.

## Signature

```python
def enforce_capability_contract(adapter: Any) -> None
```

## Parameters

<ParamField query="adapter" type="Any" required={true}>
  No description available.
</ParamField>


## Uses

- `verify_capability_contract`
- `CapabilityContractError`


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai-agents/praisonaiagents/bots/base.py#L148">
  `praisonaiagents/bots/base.py` at line 148
</Card>
