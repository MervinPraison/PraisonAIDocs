---
title: "add_tool"
description: "API documentation for PermissionAllowlist.add_tool"
icon: "function"
---

# PermissionAllowlist.add_tool

*Class: [PermissionAllowlist](../classes/PermissionAllowlist)*

Add a tool to the allowlist.

Args:
    tool_name: Name of the tool to allow
    paths: Optional list of allowed paths (empty = all paths)
    session_only: If True, permission expires with session

## Signature

```python
def add_tool(tool_name: str, paths: Optional[List[str]], session_only: bool) -> None
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `tool_name` | `str` | - |
| `paths` | `Optional` | - |
| `session_only` | `bool` | - |

## Returns

Type: `None`
