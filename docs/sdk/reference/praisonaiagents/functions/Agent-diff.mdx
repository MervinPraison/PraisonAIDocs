---
title: "diff • AI Agent SDK"
sidebarTitle: "diff"
description: "diff: Get file diffs from autonomous execution."
icon: "function"
---

# diff

<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**Agent**](../classes/Agent) class in the [**agent**](../modules/agent) module.

Get file diffs from autonomous execution.

Returns a list of :class:`FileDiff` objects showing what files
were modified, with additions/deletions counts.

## Signature

```python
def diff(from_hash: Optional[str]) -> Any
```

## Parameters

<ParamField query="from_hash" type="Optional[str]" required={false}>
  Base commit hash to diff from. If None, uses the first snapshot (pre-autonomous state).
</ParamField>

### Returns

<ResponseField name="Returns" type="Any">
  :

    agent = Agent(autonomy="full_auto")
    result = agent.start("Refactor utils.py")
    for d in agent.diff():
        print(f"&#123;d.status&#125;: &#123;d.path&#125; (+&#123;d.additions&#125;/-&#123;d.deletions&#125;)")
</ResponseField>


## Uses

- `diff`
- `logger.debug`


## Used By

- [`Agent.diff`](../functions/Agent-diff)


## Source

<Card title="View on GitHub" icon="github" href="https://github.com/MervinPraison/PraisonAI/blob/main/src/praisonai-agents/praisonaiagents/agent/agent.py#L3502">
  `praisonaiagents/agent/agent.py` at line 3502
</Card>
