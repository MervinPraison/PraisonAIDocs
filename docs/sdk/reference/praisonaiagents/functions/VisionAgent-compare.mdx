---
title: "compare • AI Agent SDK"
sidebarTitle: "compare"
description: "compare: Compare multiple images."
icon: "function"
---

# compare

<div className="flex items-center gap-2">
  <Badge color="purple">Method</Badge>
</div>

> This is a method of the [**VisionAgent**](../classes/VisionAgent) class in the [**vision_agent**](../modules/vision_agent) module.

Compare multiple images.

## Signature

```python
def compare(images: List[str], prompt: Optional[str], detail: Optional[str], model: Optional[str]) -> str
```

## Parameters

<ParamField query="images" type="List" required={true}>
  List of image URLs or file paths
</ParamField>

<ParamField query="prompt" type="Optional" required={false}>
  Custom comparison prompt
</ParamField>

<ParamField query="detail" type="Optional" required={false}>
  Detail level (low, high, auto)
</ParamField>

<ParamField query="model" type="Optional" required={false}>
  Override model for this call **kwargs: Additional parameters
</ParamField>

### Returns

<ResponseField name="Returns" type="str">
  Comparison analysis text
</ResponseField>

## Usage

```python
agent = VisionAgent()
    comparison = agent.compare([
        "before.jpg",
        "after.jpg"
    ], prompt="What changed between these images?")
```
