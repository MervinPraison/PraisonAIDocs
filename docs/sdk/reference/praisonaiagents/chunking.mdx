---
title: "Chunking Module"
description: "API reference for chunking"
icon: "code"
---

# chunking



## Import

```python
from praisonaiagents import chunking
```

## Classes

### Chunking

A unified class for text chunking with various chunking strategies.

#### Constructor Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `chunker_type` | `str` |  |
| `chunk_size` | `int` |  |
| `chunk_overlap` | `int` |  |
| `tokenizer_or_token_counter` | `str` |  |
| `embedding_model` | `Optional[Union[str, Any]]` |  |

#### Methods

- **SUPPORTED_CHUNKERS**(``) → `Dict[str, Any]`
  Lazy load chunker classes.
- **embedding_model**(``) → `Any`
  Lazy load the embedding model.
- **chunker**(``) → `Any`
  Lazy load the chunker instance.
- **chunk**(`text: Union[str, List[str]]`) → `Union[List[Any], List[List[Any]]]`
  Chunk text using the configured chunking strategy.
