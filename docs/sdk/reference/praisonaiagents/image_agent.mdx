---
title: "image_agent"
description: "ImageAgent - A specialized agent class for generating images using AI models."
icon: "image"
---

# image_agent

<Badge color="blue">Core SDK</Badge>

ImageAgent - A specialized agent class for generating images using AI models.
This class extends the base Agent class to provide specific functionality for image generation,
including support for different image models, sizes, and quality settings.

## Import

```python
from praisonaiagents import image_agent
```

## Classes

### ImageGenerationConfig

*Extends: BaseModel*

Configuration for image generation settings.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `style` | `str` |
| `response_format` | `str` |
| `timeout` | `int` |
| `api_base` | `Optional` |
| `api_key` | `Optional` |
| `api_version` | `Optional` |
</Accordion>

### ImageAgent

*Extends: Agent*

A specialized agent for generating images using AI models.

This agent extends the base Agent class with specific functionality for image generation,
including support for different models, sizes, and quality settings.

<Accordion title="Constructor Parameters">

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `name` | `Optional` | No | `None` |
| `role` | `Optional` | No | `None` |
| `goal` | `Optional` | No | `None` |
| `backstory` | `Optional` | No | `None` |
| `instructions` | `Optional` | No | `None` |
| `llm` | `Optional` | No | `None` |
| `style` | `str` | No | `'natural'` |
| `response_format` | `str` | No | `'url'` |
| `timeout` | `int` | No | `600` |
| `api_base` | `Optional` | No | `None` |
| `api_key` | `Optional` | No | `None` |
| `api_version` | `Optional` | No | `None` |
| `verbose` | `Union` | No | `True` |
</Accordion>

<Accordion title="Methods">
- **litellm**(``) → `Any`
  Lazy load litellm module when needed.
- **generate_image**(`prompt: str`) → `Dict`
  Generate an image based on the provided prompt.
- **async agenerate_image**(`prompt: str`) → `Dict`
  Async wrapper for generate_image.
- **generate**(`prompt: str`) → `Dict`
  Alias for generate_image() - for consistency with VideoAgent/AudioAgent.
- **async agenerate**(`prompt: str`) → `Dict`
  Async alias for generate_image().
- **chat**(`prompt: str`) → `Dict`
  Generate an image from the prompt.
- **async achat**(`prompt: str, temperature: float, tools: Optional`) → `Union`
  Async chat method for image generation.
- **edit**(`image: str, prompt: str, mask: Optional`) → `Dict`
  Edit an existing image with a prompt.

Args:
    image: Path or URL to the image
- **async aedit**(`image: str, prompt: str, mask: Optional`) → `Dict`
  Async version of edit().
- **variation**(`image: str, n: int, size: Optional`) → `Dict`
  Generate variations of an existing image.

Args:
    image: Path or URL to the s
- **async avariation**(`image: str, n: int, size: Optional`) → `Dict`
  Async version of variation().
</Accordion>
