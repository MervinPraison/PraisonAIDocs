---
title: "param_resolver"
description: "Unified Parameter Resolver for Consolidated Parameters."
icon: "code-fork"
---

# praisonaiagents.config.param_resolver

<Badge color="blue">Core SDK</Badge>

Unified Parameter Resolver for Consolidated Parameters.

Implements the precedence rules: Instance &gt; Config &gt; Dict &gt; Array &gt; String &gt; Bool &gt; Default

This is the SINGLE, DRY resolver used by:
- Agent
- Agents
- Workflow
- WorkflowStep

Performance: O(1) happy path. Typo suggestions only on error path.

## Overview
This module provides components for param_resolver.

## Classes

**[Dedicated Page: ArrayMode](../classes/ArrayMode)**

### ArrayMode

Array parsing modes.


## Functions

**[Dedicated Page: resolve()](../functions/resolve)**

### resolve()

Resolve a consolidated parameter following precedence rules:
Instance &gt; Config &gt; Array &gt; Dict &gt; String &gt; Bool &gt; Default

```python
def resolve(value: Any, param_name: str, config_class: Optional[Type], presets: Optional[Dict[str, Any]], default: Any, instance_check: Optional[Callable[[Any], bool]], url_schemes: Optional[Dict[str, str]], array_mode: Optional[str], string_mode: Optional[str]) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
  The parameter value
- **param_name** (`str`)
  Name of the parameter (for error messages)
- **config_class** (`Optional`)
  Expected config dataclass type
- **presets** (`Optional`)
  Dict mapping preset strings to config dicts or instances
- **default** (`Any`)
  Default value if None/unset
- **instance_check** (`Optional`)
  Function to check if value is an instance
- **url_schemes** (`Optional`)
  Dict mapping URL schemes to backend names
- **array_mode** (`Optional`)
  How to handle array values (see ArrayMode)
- **string_mode** (`Optional`)
  How to handle string values ("path_as_source", etc.)
</Expandable>


**[Dedicated Page: resolve_memory()](../functions/resolve_memory)**

### resolve_memory()

Resolve memory parameter.

```python
def resolve_memory(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_knowledge()](../functions/resolve_knowledge)**

### resolve_knowledge()

Resolve knowledge parameter.

```python
def resolve_knowledge(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_output()](../functions/resolve_output)**

### resolve_output()

Resolve output parameter.

```python
def resolve_output(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_execution()](../functions/resolve_execution)**

### resolve_execution()

Resolve execution parameter.

```python
def resolve_execution(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_web()](../functions/resolve_web)**

### resolve_web()

Resolve web parameter.

```python
def resolve_web(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_planning()](../functions/resolve_planning)**

### resolve_planning()

Resolve planning parameter.

```python
def resolve_planning(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_reflection()](../functions/resolve_reflection)**

### resolve_reflection()

Resolve reflection parameter.

```python
def resolve_reflection(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_context()](../functions/resolve_context)**

### resolve_context()

Resolve context parameter.

```python
def resolve_context(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_autonomy()](../functions/resolve_autonomy)**

### resolve_autonomy()

Resolve autonomy parameter.

```python
def resolve_autonomy(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_caching()](../functions/resolve_caching)**

### resolve_caching()

Resolve caching parameter.

```python
def resolve_caching(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_hooks()](../functions/resolve_hooks)**

### resolve_hooks()

Resolve hooks parameter.

```python
def resolve_hooks(value: Any, config_class: Optional[Type]) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Optional`)
</Expandable>


**[Dedicated Page: resolve_skills()](../functions/resolve_skills)**

### resolve_skills()

Resolve skills parameter.

```python
def resolve_skills(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_routing()](../functions/resolve_routing)**

### resolve_routing()

Resolve routing parameter (workflow steps).

```python
def resolve_routing(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_guardrails()](../functions/resolve_guardrails)**

### resolve_guardrails()

Resolve guardrails parameter.

```python
def resolve_guardrails(value: Any, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **value** (`Any`)
- **config_class** (`Type`)
</Expandable>


**[Dedicated Page: resolve_batch()](../functions/resolve_batch)**

### resolve_batch()

Resolve multiple parameters in a single batch call for performance.

This reduces function call overhead by processing all parameters together
instead of making 11+ separate resolve() calls.

```python
def resolve_batch(params: dict, presets_map: dict, config_classes: dict, defaults: dict) -> dict
```

<Expandable title="Parameters">
- **params** (`dict`)
  Dict of &#123;param_name: value&#125; to resolve
- **presets_map** (`dict`)
  Dict of &#123;param_name: presets_dict&#125;
- **config_classes** (`dict`)
  Dict of &#123;param_name: config_class&#125;
- **defaults** (`dict`)
  Dict of &#123;param_name: default_value&#125;
</Expandable>


**[Dedicated Page: resolve_guardrail_policies()](../functions/resolve_guardrail_policies)**

### resolve_guardrail_policies()

Resolve a list of policy strings into a guardrail config.

Supports policy strings like:
- "policy:strict" - Apply strict policy preset
- "pii:redact" - PII detection with redaction
- "safety:block" - Safety check with blocking

```python
def resolve_guardrail_policies(policies: list, config_class: Type) -> Any
```

<Expandable title="Parameters">
- **policies** (`list`)
  List of policy strings
- **config_class** (`Type`)
  GuardrailConfig class
</Expandable>

