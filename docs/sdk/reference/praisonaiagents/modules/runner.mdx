---
title: "runner"
description: "Hook Runner for PraisonAI Agents."
icon: "file-code"
---

# praisonaiagents.hooks.runner

<Badge color="blue">Core SDK</Badge>

Hook Runner for PraisonAI Agents.

Executes hooks registered in the registry, supporting both
Python functions and shell commands.

## Overview
This module provides components for runner.

## Classes

**[Dedicated Page: HookRunner](../classes/HookRunner)**

### HookRunner

Executes hooks from a registry.

Supports:
- Python function hooks (sync and async)
- Shell command hooks
- Sequential and parallel execution
- Timeout handling
- Result aggregation

Example:
    registry = HookRegistry()
    runner = HookRunner(registry)
    
    # Execute hooks for an event
    results = await runner.execute(
        event=HookEvent.BEFORE_TOOL,
        input_data=BeforeToolInput(
            tool_name="write_file",
            tool_input=&#123;"path": "/tmp/test.txt"&#125;
        )
    )
    
    # Check if any hook blocked
    if runner.is_blocked(results):
        print("Tool execution blocked by hook")

<Accordion title="Constructor Parameters">

| Parameter | Type | Required | Default |
|-----------|------|----------|---------|
| `registry` | `Optional` | No | `None` |
| `default_timeout` | `float` | No | `60.0` |
| `cwd` | `Optional` | No | `None` |
</Accordion>

<Accordion title="Methods">
- **registry**(``) → `HookRegistry`
  Get the hook registry.
- **async execute**(`event: HookEvent, input_data: HookInput, target: Optional`) → `List`
  Execute all hooks for an event.

Args:
    event: The event to execute hooks for
- **execute_sync**(`event: HookEvent, input_data: HookInput, target: Optional`) → `List`
  Synchronous version of execute.

Args:
    event: The event to execute hooks for
- **is_blocked**(`results: List`) → `bool`
  Check if any hook blocked execution.
- **get_blocking_reason**(`results: List`) → `Optional`
  Get the reason for blocking from results.
- **aggregate_context**(`results: List`) → `Optional`
  Aggregate additional context from all results.
</Accordion>


## Constants

| Name | Value |
|------|-------|
| `EXIT_CODE_SUCCESS` | `0` |
| `EXIT_CODE_BLOCKING_ERROR` | `2` |
| `EXIT_CODE_NON_BLOCKING_ERROR` | `1` |
