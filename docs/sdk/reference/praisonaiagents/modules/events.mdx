---
title: "events"
description: "Event-specific input types for the hook system."
icon: "bell"
---

# praisonaiagents.hooks.events

<Badge color="blue">Core SDK</Badge>

Event-specific input types for the hook system.

Each event type has its own input class with relevant fields.

## Overview
This module provides components for events.

## Classes

**[Dedicated Page: BeforeToolInput](../classes/BeforeToolInput)**

### BeforeToolInput

*Extends: HookInput*

Input for BeforeTool hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `tool_name` | `str` |
| `tool_input` | `Dict` |
| `tool_description` | `Optional` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: AfterToolInput](../classes/AfterToolInput)**

### AfterToolInput

*Extends: HookInput*

Input for AfterTool hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `tool_name` | `str` |
| `tool_input` | `Dict` |
| `tool_output` | `Any` |
| `tool_error` | `Optional` |
| `execution_time_ms` | `float` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: BeforeAgentInput](../classes/BeforeAgentInput)**

### BeforeAgentInput

*Extends: HookInput*

Input for BeforeAgent hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `prompt` | `str` |
| `conversation_history` | `List` |
| `tools_available` | `List` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: AfterAgentInput](../classes/AfterAgentInput)**

### AfterAgentInput

*Extends: HookInput*

Input for AfterAgent hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `prompt` | `str` |
| `response` | `str` |
| `tools_used` | `List` |
| `total_tokens` | `int` |
| `execution_time_ms` | `float` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: SessionStartInput](../classes/SessionStartInput)**

### SessionStartInput

*Extends: HookInput*

Input for SessionStart hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `source` | `str` |
| `session_name` | `Optional` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: SessionEndInput](../classes/SessionEndInput)**

### SessionEndInput

*Extends: HookInput*

Input for SessionEnd hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `reason` | `str` |
| `total_turns` | `int` |
| `total_tokens` | `int` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: BeforeLLMInput](../classes/BeforeLLMInput)**

### BeforeLLMInput

*Extends: HookInput*

Input for BeforeLLM hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `messages` | `List` |
| `model` | `str` |
| `temperature` | `Optional` |
| `max_tokens` | `Optional` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: AfterLLMInput](../classes/AfterLLMInput)**

### AfterLLMInput

*Extends: HookInput*

Input for AfterLLM hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `messages` | `List` |
| `response` | `str` |
| `model` | `str` |
| `tokens_used` | `int` |
| `latency_ms` | `float` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: OnErrorInput](../classes/OnErrorInput)**

### OnErrorInput

*Extends: HookInput*

Input for OnError hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `error_type` | `str` |
| `error_message` | `str` |
| `stack_trace` | `Optional` |
| `context` | `Dict` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>


**[Dedicated Page: OnRetryInput](../classes/OnRetryInput)**

### OnRetryInput

*Extends: HookInput*

Input for OnRetry hooks.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `retry_count` | `int` |
| `max_retries` | `int` |
| `error_message` | `str` |
| `operation` | `str` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
</Accordion>

