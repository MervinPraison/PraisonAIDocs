---
title: "types"
description: "Hook Types for PraisonAI Agents."
icon: "tags"
---

# praisonaiagents.hooks.types

<Badge color="blue">Core SDK</Badge>

Hook Types for PraisonAI Agents.

Defines the core types, enums, and dataclasses for the hook system.

## Overview
This module provides components for types.

## Classes

**[Dedicated Page: HookEvent](../classes/HookEvent)**

### HookEvent

*Extends: str, Enum*

Event names for the hook system.


**[Dedicated Page: HookInput](../classes/HookInput)**

### HookInput

Base hook input - common fields for all events.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `session_id` | `str` |
| `cwd` | `str` |
| `event_name` | `str` |
| `timestamp` | `str` |
| `agent_name` | `Optional` |
| `extra` | `Dict` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
  Convert to dictionary for JSON serialization.
</Accordion>


**[Dedicated Page: HookOutput](../classes/HookOutput)**

### HookOutput

Base hook output - common fields for all events.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `proceed` | `bool` |
| `stop_reason` | `Optional` |
| `suppress_output` | `bool` |
| `system_message` | `Optional` |
| `decision` | `HookDecision` |
| `reason` | `Optional` |
| `modified_data` | `Optional` |
</Accordion>

<Accordion title="Methods">
- **is_blocking**(``) → `bool`
  Check if this output represents a blocking decision.
- **should_stop**(``) → `bool`
  Check if execution should stop.
- **get_reason**(``) → `str`
  Get the effective reason for blocking or stopping.
</Accordion>


**[Dedicated Page: HookResult](../classes/HookResult)**

### HookResult

Result from a hook execution.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `decision` | `HookDecision` |
| `reason` | `Optional` |
| `modified_input` | `Optional` |
| `additional_context` | `Optional` |
| `suppress_output` | `bool` |
</Accordion>

<Accordion title="Methods">
- **is_allowed**(``) → `bool`
  Check if the result allows execution.
- **is_denied**(``) → `bool`
  Check if the result denies execution.
</Accordion>


**[Dedicated Page: HookDefinition](../classes/HookDefinition)**

### HookDefinition

Hook definition with matcher and configuration.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `id` | `str` |
| `event` | `HookEvent` |
| `matcher` | `Optional` |
| `sequential` | `bool` |
| `enabled` | `bool` |
| `name` | `Optional` |
| `description` | `Optional` |
| `timeout` | `float` |
</Accordion>

<Accordion title="Methods">
- **matches**(`target: str`) → `bool`
  Check if this hook matches the target (tool name, etc.).
</Accordion>


**[Dedicated Page: CommandHook](../classes/CommandHook)**

### CommandHook

*Extends: HookDefinition*

Hook that executes a shell command.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `command` | `str` |
| `shell` | `bool` |
| `env` | `Dict` |
</Accordion>


**[Dedicated Page: FunctionHook](../classes/FunctionHook)**

### FunctionHook

*Extends: HookDefinition*

Hook that executes a Python function.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `func` | `Optional` |
| `is_async` | `bool` |
</Accordion>


**[Dedicated Page: HookExecutionResult](../classes/HookExecutionResult)**

### HookExecutionResult

Result of executing a single hook.

<Accordion title="Properties">

| Property | Type |
|----------|------|
| `hook_id` | `str` |
| `hook_name` | `str` |
| `event` | `HookEvent` |
| `success` | `bool` |
| `output` | `Optional` |
| `stdout` | `Optional` |
| `stderr` | `Optional` |
| `exit_code` | `Optional` |
| `duration_ms` | `float` |
| `error` | `Optional` |
</Accordion>

<Accordion title="Methods">
- **to_dict**(``) → `Dict`
  Convert to dictionary.
</Accordion>

