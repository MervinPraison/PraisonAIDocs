---
title: "Env"
description: "Environment and diagnostics information"
icon: "server"
---

The `env` command displays environment and diagnostics information.

## Usage

```bash
praisonai env [OPTIONS] COMMAND [ARGS]...
```

## Commands

| Command | Description |
|---------|-------------|
| `show` | Show environment variables |
| `check` | Check environment configuration |

## Examples

### Show environment

```bash
praisonai env show
```

### Check configuration

```bash
praisonai env check
```

## Related Environment Variables

PraisonAI supports several environment variables for configuration and security:

| Variable | Purpose |
|----------|---------|
| `PRAISONAI_ALLOW_LOCAL_TOOLS` | Enable local tools.py file loading |
| `PRAISONAI_ALLOW_JOB_WORKFLOWS` | Enable job workflow execution |
| `PRAISONAI_BROWSER_ALLOW_REMOTE` | Enable remote browser access |
| `PRAISONAI_RUN_SYNC_TIMEOUT` | Default timeout (seconds, default `300`) for the wrapper `run_sync` bridge **and** for the `praisonai-code` agent-centric tool bridge — bounds ACP/LSP tool calls so a hung tool cannot block the agent. See [Agent-Centric Tools → Timeouts & Cancellation](/cli/agent-tools#timeouts-cancellation). |
| `PRAISONAI_SESSION_CACHE_MAX` | Max `PersistenceOrchestrator` in-memory session cache size (LRU, default `1024`). See [Persistence Overview → Session Cache Size](/persistence/overview#session-cache-size). |

For complete details and security implications, see [Security Environment Variables](/features/security-environment-variables).

## See Also

- [Config](/cli/config) - Configuration management
- [Doctor](/cli/doctor) - Health checks
