---
title: "Backends"
sidebarTitle: "Backends"
description: "List registered external coding-CLI backends available to scheduled jobs and agent runtimes"
icon: "plug"
---

External coding-CLI backends (delegate agent turns to a CLI).

```mermaid
graph LR
    Cmd[💻 praisonai backends] --> Registry[🔌 Backend registry]
    Registry --> Ids[📋 built-ins + entry-point plugins]

    classDef input fill:#6366F1,stroke:#7C90A0,color:#fff
    classDef process fill:#189AB4,stroke:#7C90A0,color:#fff
    classDef output fill:#10B981,stroke:#7C90A0,color:#fff
    class Cmd input
    class Registry process
    class Ids output
```

## Usage

```bash
praisonai backends           # default: lists ids
praisonai backends list      # explicit
```

Both forms print each registered backend id on its own line:

```text
claude-code
codex-cli
gemini
grok
```

## Commands

| Command | Description |
|---------|-------------|
| `backends` | Same as `list` (list is the default). |
| `backends list` | List registered CLI backend ids (built-ins plus entry-point plugins). |

<Note>
Available when `praisonai-code` is installed (standalone `praisonai-code` installs also expose this). Legacy argparse listing remains wrapper-resident.
</Note>

## See Also

- [Scheduler Backend Action](/features/scheduler-backend-action) — run a scheduled job as one headless CLI-backend turn
- [CLI Backend Protocol](/features/cli-backend-protocol) — how CLI backends plug into the Agent API
- [Runtime Selection](/features/runtime-selection) — model-scoped runtime configuration
- [Claude Code](/code/claude-code) — the flagship CLI backend
- [Codex CLI](/code/codex-cli) — ChatGPT-subscription backend
- [Gemini CLI](/code/gemini-cli) — Google-account backend
- [Grok CLI](/code/grok-cli) — xAI-subscription backend
