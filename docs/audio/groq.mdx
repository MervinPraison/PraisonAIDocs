---
title: Groq Audio
description: Fast speech-to-text with Groq
icon: bolt
---

Ultra-fast transcription using Groq's Whisper implementation.

## Setup

```bash
export GROQ_API_KEY=your-key
```

## Usage

```python
from praisonaiagents import AudioAgent

agent = AudioAgent(llm="groq/whisper-large-v3")
text = agent.transcribe("audio.mp3")
print(text)
```

## Models

| Model | Description |
|-------|-------------|
| `groq/whisper-large-v3` | Whisper Large v3 |
| `groq/whisper-large-v3-turbo` | Faster variant |
| `groq/distil-whisper-large-v3-en` | English only |

## With Language

```python
text = agent.transcribe("audio.mp3", language="es")
```

<Tip>
Groq offers the fastest Whisper inference available.
</Tip>
